# G 题 机-车通信接口约定

> 2023 电赛 G 题 空地协同智能消防系统。
> 无人机侧仓库：`kian_26fly`(分支 `fire-task`)，消防车侧仓库：`diansai_H_car`。
> 两块板 `ROS_DOMAIN_ID` 不同（**车 6 / 机 26**），**控制面一律走 UDP**（本文四条通道）。
>
> **2026-07-23 新增第五条通道：视频流走 DDS 跨域桥**，见 §十。控制面不受影响、一行未改。
>
> 本文是两边的**唯一契约**。任何一侧改包格式/端口，必须同步改本文和另一侧。

---

# 【当前状态】两侧均已实现

四条通道**两边代码都已落地**。本节记录进度，接口细节见后面各章。

| 通道 | 端口 | 无人机侧 | 消防车侧 |
|---|---|---|---|
| 火源坐标上报 | 车 :8889 | ✅ `fire_link_pkg` | ✅ `fire_event_bridge` |
| 任务状态回传 | 机 :8890 | ✅ `fire_link_pkg` | ✅ `fire_event_bridge::reply()` |
| 遥测（位置/里程/高度/阶段） | 车 :8892 | ✅ `fire_link_pkg` | ✅ `fire_link_bridge` |
| 按键启动 | 机 :8893 | ✅ `fire_link_pkg` | ✅ `fire_link_bridge` |
| 车载显示器 | — | — | ✅ `fire_dashboard.py` |
| **视频流（§十）** | **DDS 域6** | ✅ `fire_video_link_pkg` | ⬜ 队友订阅即可，无需写代码 |

### ⚠️ 但是：全部未经真机验证

| 项 | 状态 |
|---|---|
| 无人机侧编译 | ✅ 已在开发板 `colcon build` 通过（2026-07-22） |
| 消防车侧编译 | ✅ 队友已在 Orange Pi 5 Max 上构建通过，B1~B4 四个 bug 全修（见其 README） |
| 链路对拉 | ❌ 两机之间从未跑通过一次真实收发（两边都只做了本机自环） |
| 场地原点标定 | ❌ 两边都是 0 / 默认值 |
| 车端激光 | ✅ 已接实 GPIO（`mock_mode: false`，GPIO1_B0/物理22脚，低电平开，3s 超时）— 未上电实测 |
| 车端街区坐标 | ❌ `obstacles_dm` 仍是照赛题图估的 |
| 车端物理按键 | ❌ `button_gpio_value_path` 还是空，只能靠仪表盘空格/回车（题目要求"车上按键"） |
| 车端开机自启 | ❌ 未装 systemd/桌面自启项 |
| seq 去重重启即卡死 | ❌ 见 §二 红框，**车端两处都要补"3s 无包即复位"** |
| 无人机试飞 | ❌ 主链路一次没飞过 |

**下一步优先级：先各自上板 `colcon build`，再用 §八 的自测手段把链路对拉通。**
代码写得再对，没跑过就等于零。

## 零、总原则：坐标一律用「场地系 dm」

- 原点 = 题目图 1 **巡防区左下角**，右上角 = (48, 40)，单位 **dm**。
- 跨机传的所有坐标都是这个系，**两边收到即可直接用，不做任何二次换算**。
- 各自的 Cartographer `map` 系与场地系的标定各自负责：
  - 无人机侧：`fire_control_pkg` 的 `home_field_x_dm / home_field_y_dm / field_yaw_offset_deg`
  - 消防车侧：`fire_params.yaml` 的 `arena_origin_map_x_m / y_m / yaw_deg`

> ⚠️ **两边必须标到同一个物理原点**（同一个场地左下角）。任一侧标偏，火源坐标传过去就是错的，
> 车会开到错误位置。建议赛前用同一个基准点、同一把尺子，两边各标一次后互相验证：
> 把飞机停在某个已知格点，看车端显示的坐标对不对。

---

## 一、网络

**两机接同一台路由器**，同网段直连。

> 组网方案变更史（避免翻旧记录）：
> ① 外接路由器静态 IP（`.161`/`.171`）→ ② 车板自建热点 `AIC800-Hotspot`
> → ③ **回到路由器共享局域网（当前）**。②因车板热点实测效果差被弃用。
> 后续计划改**蓝牙**，但那是本轮比赛之后的事。

| 项 | 值 |
|---|---|
| 网段 | `192.168.10.x` |
| **消防车** | **`192.168.10.113`（固定）** |
| **无人机** | **`192.168.10.197`（固定）** |

两端都是固定 IP，所以两个方向都直接发往对方地址，不需要地址发现。

> ### 车端地址解析：**最终实现是"只用静态参数"**（原三级解析已被移除）
>
> 车端 `fire_link_bridge` 现在只有一条路：`drone_ip` 参数（已填 `192.168.10.197`）+
> `start_udp_port`，启动时 `inet_pton` 解析失败会直接抛异常退出。
> 学地址和子网广播两条兜底**被队友明确删掉了**，并写进了他们那份接口文档。
>
> 后果要心里有数：**`sendto` 发到错误地址不报错**，所以万一 IP 配错/网卡换了，
> 现场表现就是"按了键飞机毫无反应"，且车端日志一切正常。
> 排查顺序固定为：`ip a` 看车是不是 `.113` → `ping 192.168.10.197` →
> `tcpdump -ni any udp port 8893` 看包有没有真发出去。

> **未来换蓝牙时**：只需替换两侧桥节点的收发实现，
> `/fire_start`、`/car_status`、`/drone_telemetry`、`/fire_event` 这些**话题接口不变**，
> 上下游一行不用动——这也是当初把传输层单独拆成桥节点的原因。

### 端口分配（**不要重复使用**）

| 端口 | bind 方 | 用途 | 状态 |
|---|---|---|---|
| 8889 | 车 | 机→车 **火源坐标上报**（16B `0xFC11`） | ✅ 两侧均已实现 |
| 8890 | 机 | 车→机 **任务状态回传**（裸 ASCII 字符串） | ✅ 两侧均已实现 |
| 8892 | 车 | 机→车 **遥测**（32B `0xF14E` type=1） | ✅ 两侧均已实现 |
| 8893 | 机 | 车→机 **按键启动**（32B `0xF14E` type=3） | ✅ 两侧均已实现 |
| ~~8888~~ | — | 旧跟随链路占用，本题不要用 | |

> 防火墙：确认两块板都没有拦截 UDP **8889 / 8890 / 8892 / 8893** 入站。
> 路由器若开了 AP 隔离（client isolation），两机会互相 ping 不通——**先 ping 一下再查代码**。

---

## 二、通道 1：火源坐标上报（机 → 车 :8889）

**车端零改动。** 无人机侧已按你们现有的 `fire_event_bridge` 格式来发。

小端定长 **16 字节**：

```
偏移  类型      字段        说明
0     uint16   magic       固定 0xFC11
2     uint16   seq         递增，接收端按 int16 差值判新旧（车端已实现）
4     float32  x_dm        火源 x，场地系 dm
8     float32  y_dm        火源 y，场地系 dm
12    float32  reserved    置 0
```

> ### 🔴 seq 去重有个会吃掉整场比赛的坑：**发送端重启后 seq 归零**
>
> 无人机侧 `report_seq_` 是节点成员，**每次重启从 0 开始**，而全场只报一次火源，
> 所以每一轮跑的火源包 seq 永远都是 **0**。
> 车端现在的判据是 `have_seq_ && int16(seq - last_seq_) <= 0 → 丢弃`：
>
> - 第 1 轮：车端没收过，接受，`last_seq_=0` ✅
> - 第 2 轮（飞机重启、车端没重启）：`int16(0-0)=0 ≤ 0` → **丢弃 → 车永远不出发** ❌
>
> 也就是说：**只要车端不跟着重启，调试跑过一次之后，正式那次的火源上报会被静默丢掉。**
> 遥测（§四）同理，只是表现为"飞机重启后车端屏幕永久停更"——车端 `last_seq_` 停在几百，
> 飞机重新从 0 数，要几百秒后才追上。
>
> **修法（车端两处都要，机端不用改）**：在 seq 判据上并一个"流中断即复位"的条件——
> 距上一个有效包超过 `link_timeout_s`（3s）时，把 `have_seq_` 置回 false，
> 当作新的一条流重新起判。这样既保留乱序去重，又能自动吃掉发送端重启。
>
> 现场应急兜底：**每次起飞前先重启车端 launch**（车端一重启 `have_seq_` 就清了）。

行为约定：

- 无人机**报两次**（2026-07-22 改）：
  1. **视觉确认火源的那一刻**就报第一次 —— 不等抛包。抛包流程（接近→降高→悬停3s→抛）
     中任何一步卡住，坐标就永远发不出去，而车端收不到坐标 = 发挥 2/3 的 25 分全丢。
     况且车开过去本来就要时间，早报只会更快。
  2. **抛完灭火包后**再报一次同样的坐标，作为第一次全丢时的补救。
- 每一次都是**同一个 seq 连发 5 次**做丢包冗余（冗余在 `fire_link_node` 里做，
  两次上报的 seq 不同，坐标相同）。
- 车端按 seq 去重，且 `on_fire()` 里有 `state_ != IDLE` 的保护 —— 第二次上报到达时
  车已经在路上，会被直接忽略，**不会重复出发或重新规划**（已核对车端代码）。
- 全场只报这两次（三个图案里只有目标是红色，无人机处理完即不再上报）。

---

## 三、通道 2：任务状态回传（车 → 机 :8890）

**车端零改动。** 你们现有的 `fire_event_bridge::reply()` 把 `/fire_mission_status`
的字符串原样 sendto 到「最近一个发火源包的 IP」的 8890 口。无人机侧会 bind 8890 接收。

请**确认**取值集合仍是这几个（无人机侧按这个表解析，用于在机上状态里显示车干到哪了）：

```
ready / enroute / extinguishing / returning / done / failed:<reason>
```

> ⚠️ **注意这条通道的一个隐含行为**：你们的 `reply()` 是发给「最近一个发过火源包的 IP」，
> 所以在无人机上报火源之前，`have_sender_` 一直是 false，**机端收不到任何状态**。
> 这不是 bug，但别误以为一上电就双向通了。如果希望全程可见车端状态，
> 需要改成从参数里读固定的无人机 IP——不过这对拿分没影响，优先级很低。

建议（不强制）：`done` 和 `failed:*` 这两个终态**连发 3 次**，因为它们是一次性事件，
丢了机端就永远不知道车完成了。中间态是周期性的，丢一两个无所谓。

---

## 四、通道 3：遥测（机 → 车 :8892）

**这条对应基本要求第 3、4 项，共 16 分。**

小端定长 **32 字节**：

```
偏移  类型      字段          说明
0     uint16   magic         固定 0xF14E
2     uint8    type          固定 1 (TELEMETRY)
3     uint8    phase         无人机任务阶段，见下表
4     uint16   seq           递增，按 int16 差值判新旧，丢弃乱序旧包
6     uint16   reserved16    置 0
8     uint32   stamp_ms      发送方单调时钟 ms。**不要做跨机时钟同步**，
                             判超时只用「收包本地时间」
12    float32  x_dm          无人机位置 x，场地系 dm
16    float32  y_dm          无人机位置 y，场地系 dm
20    float32  distance_dm   累计巡逻里程 dm
24    float32  height_dm     离地高度 dm
28    uint32   reserved32    置 0
```

C 结构体（加 `__attribute__((packed))`，`static_assert(sizeof(...) == 32)`）：

```cpp
struct __attribute__((packed)) FireLinkPacket {
  uint16_t magic;       // 0xF14E
  uint8_t  type;        // 1=TELEMETRY, 3=CAR_START
  uint8_t  phase;
  uint16_t seq;
  uint16_t reserved16;
  uint32_t stamp_ms;
  float    x_dm;
  float    y_dm;
  float    distance_dm;
  float    height_dm;
  uint32_t reserved32;
};
static_assert(sizeof(FireLinkPacket) == 32, "must be 32 bytes");
```

Python 解析格式串（自测用）：`"<HBBHHIffffI"`，`struct.calcsize` 必须等于 32。

### phase 取值

| 值 | 含义 | 值 | 含义 |
|---|---|---|---|
| 0 | 待命（等按键） | 6 | 抛包 |
| 1 | 起飞 | 7 | 恢复巡逻 |
| 2 | 巡逻 | 8 | 返航 |
| 3 | 接近火源 | 9 | 降落 |
| 4 | 降高 | 10 | 完成 |
| 5 | 悬停 | 255 | 未知 |

### 发送频率

题目原文是「巡逻期间无人机每秒向消防车发送 1 次位置坐标信息」，故**默认 1Hz**。
无人机侧有参数 `telemetry_rate_hz` 可调高（多发不违规、显示更跟手），联调时可以先开到 5Hz
看链路稳不稳，正式测试再决定。

### 车端接收侧要做什么

1. bind UDP 8892，非阻塞轮询（20ms 一次即可）。
2. 校验 `长度 == 32` 且 `magic == 0xF14E`，**按 seq 用 int16 差值判新旧**，丢弃乱序旧包
   （写法照抄你们 `fire_event_bridge::poll()` 里那一行即可，逻辑完全一样）。
   ⚠️ **务必按 §二 那个红框加"超过 3s 没收包就复位 seq 状态"**，否则无人机一重启，
   车端屏幕会停更几百秒（seq 归零，要等飞机数回去才恢复）。
3. 发布成一个 ROS 话题（名字你们定，比如 `/drone_telemetry`），供显示节点订阅。

显示部分见下一节，那是独立的一大块。

---

## 五、通道 4：按键启动（车 → 机 :8893）

**这对应基本要求第 2 项「在消防车上按键启动无人机」。**

同一个 32 字节结构体，只是字段填法不同：

```
magic       = 0xF14E
type        = 3   (CAR_START)
phase       = 255
seq         = 递增
stamp_ms    = 发送时刻
x_dm / y_dm / distance_dm / height_dm = 0
reserved32  = 0
```

发到无人机的 `192.168.10.197:8893`（车端 `drone_ip` / `start_udp_port` 参数，见 §一）。

行为约定：

- 按键按下 → **连发 5 次**（间隔几 ms 即可），因为这是一次性事件，丢了飞机就不起飞。
- 无人机侧**只认第一次**，后续重复包全部忽略，不会重复触发。所以你们连发多少次都安全。
- 按键去抖由车端负责。飞机起飞后再按无效。

> 硬件上按键接哪个 GPIO / 用什么方式触发由你们定，飞机侧只关心这个 UDP 包。
> 如果一时接不上物理按键，先做成一个 ROS topic 或命令行触发也行，联调不受影响。

---

## 六、消防车显示器

**对应基本要求第 3、4 项，共 16 分。这是车端工作量最大、也最值钱的一块。**

前提：车上自带屏幕，直接在车载板子上开一个**全屏窗口**显示，不要用笔记本远程看
——题目原文是「消防车上显示器」，远程画面大概率不认。

### 必须显示的内容

| # | 内容 | 数据来源 | 对应要求 |
|---|---|---|---|
| 1 | 无人机**实时位置坐标**（数字，dm） | 遥测包 `x_dm / y_dm` | 基本要求 3 |
| 2 | 无人机**巡逻航迹曲线** | 历史遥测点连线 | 基本要求 4 |
| 3 | **累计巡逻里程**（数字，dm） | 遥测包 `distance_dm` | 基本要求 4 |
| 4 | 火源地点坐标（收到后常驻显示） | `/fire_event` | 发挥 2/3 |

> **里程直接用包里的 `distance_dm`，不要自己拿收到的点去积分。**
> 机端是用 Cartographer 位姿增量（约 20Hz）算的，你按 1Hz 采样点连线积分会显著偏小
> （拐弯处尤其）。想对照的话可以两个都显示，但报给评委的以包里的为准。

### 航迹曲线的具体要求

评委实际看的就是这张图，光有一条线和画在场地图上，观感差很多：

- **按比例画整个巡防区**：48dm(宽) × 40dm(高)，长宽比必须对，别拉伸。
- **底图要有**：
  - 六个白色街区矩形——直接读你们 `fire_params.yaml` 里的 `obstacles_dm`，
    一份数据两处用，改场地标定时不会漏改
  - 左下无人机起降区（黑色，11×7dm）
  - 消防车出发区（红色，`home_x_dm/home_y_dm` 附近）
- **坐标方向**：原点在**左下角**，x 向右，y 向上。屏幕像素 y 轴是向下的，
  记得翻转，否则整张图上下颠倒。
- **航迹**：历史点按时间顺序连成折线，实时追加；当前位置用一个明显的标记（圆点/十字）。
- **火源**：收到火源坐标后在图上打一个显眼的 ★，并标注坐标数字。
- **消防车自己的位置**：如果车端 TF 可用，一并画出来（不是要求项，但演示效果好很多）。

### 文字区

字号要大——评委是**站着看**的，别用默认小字。至少包含：

```
无人机  (12.4, 20.0) dm   高度 18.2dm   阶段: 巡逻
里程    124.5 dm
消防车  enroute
火源    (30.0, 25.0) dm
链路    正常 / ⚠ 离线 3.2s
```

- **`phase` 和 `height_dm` 必须显示**。比赛时你们只能看这块屏，这两个是判断飞机是否正常的
  唯一手段——`phase` 长时间不变基本就是飞机卡住了，得准备停机。
- **链路健康**：超过 3 秒没收到遥测包就显示红色「离线」和断连时长。
  UDP 没有连接概念，不显示这个的话链路断了你们会毫无察觉，还以为飞机在正常巡逻。

### 技术选型建议

不强制，选你们上手最快的：

- **Python + pygame** 全屏窗口——画矩形/折线/文字都是一行，最省事，推荐
- **Python + OpenCV** `cv2.imshow` + `WINDOW_FULLSCREEN`——如果你们已经熟 OpenCV
- **rqt / rviz**——能画轨迹但排版丑、字小、街区底图要额外做 Marker，不推荐给评委看

无论选哪个：**做成开机自启**，比赛现场没时间敲命令。

### 验收标准

用 §八 的假遥测脚本喂数据，屏幕上应该看到：

1. 一个点沿 y=4 那条带从左往右移动，航迹连成一条直线
2. 里程数字线性增长
3. 停掉脚本 3 秒后，链路状态变红显示「离线」
4. 整张场地图长宽比正确、街区位置与实际场地对得上、原点在左下

---

## 七、一次完整测试的时序

```
1. 两板上电，各自 Cartographer 起来，TF 正常
2. [车] 操作员按下启动键
   → 车发 CAR_START ×5 → 机 :8893
3. [机] 收到 → 起飞到 18dm → 开始蛇形巡逻
   → 从此刻起每秒发 TELEMETRY → 车 :8892
   → 车端屏幕开始画航迹曲线、刷坐标和里程
4. [机] 巡逻中下视相机发现红色火花图案 → 多帧投票确认
   → **立刻发火源坐标 0xFC11 ×5 → 车 :8889**（车可以同时出发，不等抛包）
   → 机上 LED 报警 → 接近 → 降到 10dm → 悬停 3s → 抛灭火包
   → 抛完再发一次同坐标（车已在路上，会被忽略）
5. [车] 收到火源坐标 → 显示 → BFS 绕开街区 → 开到照射位
   → 激光照射 2.1s → 返回红色出发区
   → 全程回传状态字符串 → 机 :8890
6. [机] 抛完继续巡逻 → 覆盖完成 → 返航 → 降落在起飞区内
   → phase 变 10(完成)
7. 发挥部分限时 360s
```

---

## 八、联调自测（不需要真飞机 / 不需要真车）

无人机侧仓库里有 `scripts/fire_link_test.py`，它**站在消防车的位置**收发包，
可以直接当车端接收逻辑的参考实现。

```bash
# 在车板上跑（或本机跑，把 --drone-ip 换成 127.0.0.1 自环）
./fire_link_test.py --start-after 3          # 默认 --drone-ip 192.168.10.197，本机自环用 127.0.0.1
```

它会：bind 8892 收遥测并打印位置/里程/高度/阶段，`--start-after` 秒后向飞机 8893 连发启动包。
默认端口已按本文档设好，不需要额外覆盖。

**做任务 1（按键启动）时可以这样自测**，完全不依赖飞机侧改完：
车端按键逻辑写好后，让它发包到 `127.0.0.1:8893`，同时本机跑
`./fire_link_test.py --drone-ip 127.0.0.1` 之外再起一个 socket 收 8893 验证包对不对；
或者更简单——直接用本脚本的 `make_start_packet()` 当标准答案比对字节。

反向的最小验证（车端写好后，不用飞机）：

```bash
# 假装飞机在发遥测
python3 -c "
import socket,struct,time
s=socket.socket(socket.AF_INET,socket.SOCK_DGRAM)
for i in range(100):
    s.sendto(struct.pack('<HBBHHIffffI',0xF14E,1,2,i,0,int(time.monotonic()*1000)&0xFFFFFFFF,
             10.0+i*0.4,4.0,i*4.0,18.0,0),('192.168.10.113',8892))
    time.sleep(1)
"
```

车端屏幕应该看到一个点沿 y=4 那条带从 x=10 往右走，航迹画成一条直线，里程线性增长。

---

## 九、车端待办清单

> 2026-07-22 复核过队友仓库 `diansai_H_car`（commit `6fb0793`）后重排，✅ 的不用再看。

### 软件已完成（本机自环验证过）

- [x] **按键 → UDP 8893 发启动包**（§五）—— 但只能软触发，见下面"物理按键"
- [x] **UDP 8892 接收遥测**（§四）+ 屏幕显示实时坐标（§六，基本要求 3，**8 分**）
- [x] **屏幕画巡逻航迹曲线 + 累计里程 + 阶段/高度/链路状态**（§六，基本要求 4，**8 分**）
- [x] `fire_dashboard.py` 的 B1 崩溃、B2 中文字体 WARN、B4 `stamp_ms` 精度三个 bug 已修
- [x] `laser_gpio_driver` 已接实 GPIO（`mock_mode: false`）

### 必做（还差这些才拿得到分）

- [ ] 🔴 **seq 去重加"3s 无包即复位"**（§二 红框）—— 不改的话飞机重启后火源包会被静默丢弃
- [ ] 🔴 **接物理启动按键**：`button_gpio_value_path` 还是空，现在只能在仪表盘按空格/回车
      （= 比赛时车上得插一块键盘）。sysfs 路径填上就行，代码和 README 都已就绪
- [ ] 🔴 显示程序 + 整套 launch **开机自启**（systemd/桌面自启项，队友 README 里也列了未配置）
- [ ] `fire_params.yaml` 的 `obstacles_dm` 是**照 PDF 图估的**，必须现场量六个街区的实际边界
- [ ] `arena_origin_map_x_m / y_m / yaw_deg` **全是 0，没标定** —— 不标车会开到错误位置
- [ ] `home_x_dm=13.5, home_y_dm=2.5`（红色出发区）需现场核对
- [ ] 激光实测：上电默认 OFF、`max_on_s=3.0` 超时 OFF、节点退出 OFF、故障 OFF（代码已写，没上电验过）
- [ ] 实车验证：控制点偏移、到点容差、1.5dm 安全余量下车轮**不压街区边界线**（压线扣分）
- [ ] 建议：`done` / `failed:*` 终态**连发 3 次**（现在单发，丢了机端永远不知道车干完了）

### 两边一起做的（缺了火源坐标就是错的）

- [ ] **场地原点联合标定**：两边各自标 `map` → 场地系，然后互验（见 §零）。
      验证方法：把飞机停在某个已知格点，看车端屏幕显示的坐标对不对。

### 确认项（回我一句就行）

- [ ] 路由器有没有开 AP 隔离（两机能否互相 ping 通）
- [x] 状态字符串取值集合确认为 `ready/enroute/extinguishing/returning/done/failed:<reason>`
      （`busy` 已被队友去掉，忙时重发当前合法状态）
- [ ] 8892 / 8893 这两个端口有没有和你们别的东西冲突
- [ ] 车载屏幕的**分辨率**是多少（仪表盘已验 1280×720 / 800×480，真屏全屏效果还没看过）

---

## 十、通道 5：视频流（DDS 跨域桥，机 → 车）

> 2026-07-23 新增。**这是唯一不走 UDP 的通道**，其余四条一行未改。

### 为什么不走 UDP

视频帧塞不进 32 字节定长包，自己在 UDP 上做分片重组等于重写一遍传输层。
实测 DDS 传图像效果最好，所以单开一条 DDS 通道，与四条 UDP 通道并存。

### 原理：一个进程、两个域

`ROS_DOMAIN_ID` 是**进程级**的，ROS 2 没有"给单个话题指定域"的机制。
但一个进程里可以建多个 `rclcpp::Context`，每个绑一个域。无人机侧的
`fire_video_link_pkg/fire_video_bridge_node` 同时是域 26 和域 6 的成员，
把指定话题从前者转发到后者。

**只有下表列出的话题会出现在域 6 上**，无人机其余几十个话题
（`/target_position`、`/height`…）继续留在域 26，车端看不见，也不会和车端话题重名。

### 车端要做什么：只订阅，不用写代码

| 域 6 上的话题 | 类型 | 内容 |
|---|---|---|
| `/fly/fire/debug_image/compressed` | `sensor_msgs/msg/CompressedImage` | JPEG 火源识别调试图，带检测框 |

```bash
# 车端（本来就在域 6）直接看
ros2 topic hz /fly/fire/debug_image/compressed
ros2 run rqt_image_view rqt_image_view /fly/fire/debug_image/compressed
```

`/fly` 前缀是桥加的，专门用来跟车端自己的话题分开。

### 带宽约定（**重要，别改大**）

优先级是死的：**UDP 控制包 > 视频流帧率 > 画质**。控制包丢了要丢分，视频糊一点没人扣分。

| 项 | 值 | 在哪改 |
|---|---|---|
| 编码 | JPEG | — |
| 分辨率 | 宽 ≤ 320 | `debug_image_max_width` |
| 帧率 | ≤ 10 Hz | `debug_image_max_hz` |
| 码率上限 | 800 kbps | `debug_image_max_kbps` |
| JPEG 质量 | 50，超码率自动降到最低 15 | `debug_image_jpeg_quality` |

参数都在无人机侧 `fire_vision_pkg` 的 `fire_detector` 节点上。超码率时**降画质、不降帧率**。

QoS 用 `best_effort` + 深度 1：图像丢帧无所谓，而 `reliable` 的重传会跟
UDP 控制包抢无线带宽，那才是要命的。**车端订阅也必须用 `best_effort`**
（reliable 的订阅收不到 best_effort 的发布，会表现为一帧都收不到）。

### 启动

无人机侧（`fire_full.launch.py` 未包含，需单独起）：

```bash
source scripts/setup_dds.sh
ros2 launch fire_video_link_pkg fire_video_bridge.launch.py
```

### 排障

桥每 5 秒打一行吞吐，先看它就能分清故障在哪一侧：

```
[桥] /fire/debug_image/compressed → /fly/fire/debug_image/compressed  9.8 Hz  620 KB/s
```

- 打 `近 5s 无数据` → 入口没收到，问题在无人机内部（检测节点没起 / `enable_debug_image` 没开），与网络无关
- 有数但车端收不到 → 网络或 QoS：先确认车端订阅是 `best_effort`，再查路由器是否转发 DDS 多播
- 廉价 AP 常不转发 Wi-Fi 多播，表现为 `ping` 通但 `ros2 topic list` 互相看不见。
  两边 IP 本来就固定（车 `192.168.10.113` / 机 `192.168.10.197`），必要时给 FastDDS 配单播发现。

完整测试脚本：`scripts/video_link_test.py`。无人机侧 `python3 scripts/video_link_test.py` 做三段体检；
车端验收 `python3 video_link_test.py --dst-only`（脚本可直接拷过去，只依赖 rclpy）。
