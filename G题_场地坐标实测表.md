# G 题 场地坐标实测表

> 2023 电赛 G 题 空地协同智能消防系统。
> 本文给出巡防区内**所有固定区域**在场地系下的坐标，机端（`kian_26fly`）、车端（`diansai_H_car`）
> 共用这一份数据源，不要再各自照图目测。
>
> 坐标系与《G题_机车通信接口约定.md》§零 一致：**原点 = 巡防区左下角，右上角 = (48, 40)，单位 dm**。

---

## ⚠️ 先看这里：车端 `obstacles_dm` 有一处对不上

车端参数/注释里记的是 **左中街区 = `x 6~15 / y 7~20`**，实测是 **`x 6~14 / y 11~20`**。

| | 车端现值 | 实测 | 差 |
|---|---|---|---|
| x 右沿 | 15 | **14** | 大 1dm |
| y 下沿 | **7** | **11** | **大 4dm** |

方向上是**把障碍框大了**，不会撞，但 `y 7~11` 那条 8dm×4dm 的空地被当成了障碍，
车端规划会平白绕远路，极端情况下会认为"无通路"。

不建议只改这一块 —— 下面给全套，整体替换更稳妥。

---

## 一、区域坐标（场地系 dm）

### 1）地面标记区

| 区域 | 颜色 | x | y | 尺寸 | 备注 |
|---|---|---|---|---|---|
| 无人机起降区 | 黑 | **0 ~ 7** | **0 ~ 7** | **7 × 7** | 中心 (3.5, 3.5) |
| 消防车停车区 | 红 | 11 ~ 16 | 0 ~ 5 | 5 × 5 | 与火源图案同色，视觉需靠几何黑名单排除 |

> 🔴 **起降区是 7×7dm，不是 11×7dm。**
> 题目图上标注的那个 `11dm`，量的是**原点到红色停车区左沿**的距离，不是黑块宽度；
> 上方另一个 `7dm` 才是黑块宽度。这两个标注挨得很近，极易读错。
>
> 机端 `fire_control_pkg` 原先把起飞点中心按 11×7 算成了 (5.5, 3.5)，已修正为 **(3.5, 3.5)**；
> `fire_vision_pkg` 的几何黑名单也从 `[0,0,11,7]` 收成了 `[0,0,7,7]`。
> **车端若有任何地方按 11×7 理解过起降区，请一并核对。**

### 2）街区（白色块，共 6 个）

下表为**外接矩形**（包围盒）。实际形状是四角切角的八边形，按矩形处理偏保守，安全方向没问题。

| # | 位置 | x | y | 尺寸 |
|---|---|---|---|---|
| 1 | 左上 | 6 ~ 17 | 26 ~ 35 | 11 × 9 |
| 2 | 中上 | 23 ~ 31 | 26 ~ 35 | 8 × 9 |
| 3 | 右上 | 37 ~ 45 | 26 ~ 35 | 8 × 9 |
| 4 | 左中 | **6 ~ 14** | **11 ~ 20** | 8 × 9 |
| 5 | 中中 | 20 ~ 31 | 11 ~ 20 | 11 × 9 |
| 6 | 右（高） | 37 ~ 45 | 5 ~ 20 | 8 × 15 |

车端 `obstacles_dm` 可直接按 `[xmin, ymin, xmax, ymax]` 顺序填：

```yaml
obstacles_dm: [
   6.0, 26.0, 17.0, 35.0,   # 1 左上
  23.0, 26.0, 31.0, 35.0,   # 2 中上
  37.0, 26.0, 45.0, 35.0,   # 3 右上
   6.0, 11.0, 14.0, 20.0,   # 4 左中  ← 原为 6,7,15,20，y 下沿差 4dm
  20.0, 11.0, 31.0, 20.0,   # 5 中中
  37.0,  5.0, 45.0, 20.0,   # 6 右（高）
]
```

### 3）火源图案

**不写死坐标。** 题目图上画的三个红色星是示意，实际比赛现场位置未知，
必须靠无人机视觉现场识别后上报。车端不要预置任何火源位置。

只需记住一个约束：**火源放在街区里**。所以视觉侧的几何黑名单绝不能盖到街区范围，
黑名单只允许框住上面第 1 节那两块地面标记区。

---

## 二、这些数是怎么来的

从题目图 1 直接按像素反算，不是卷尺量的：

- 网格横向 `x: 88px → 1187px` 对应 `0 → 48dm`
- 网格纵向 `y: 1043px → 121px` 对应 `0 → 40dm`
- 两个方向都约 **23 px/dm**，比例自洽

各区域用颜色阈值分割后取连通域包围盒，再换算成 dm。原始测量值（未取整）：

| 区域 | 实测 x | 实测 y |
|---|---|---|
| 起降区（黑） | 0.04 ~ 7.03 | 0.17 ~ 7.11 |
| 停车区（红） | 11.09 ~ 15.94 | 0.26 ~ 5.08 |
| 街区 1 | 6.20 ~ 17.03 | 26.07 ~ 34.71 |
| 街区 2 | 23.15 ~ 30.79 | 26.07 ~ 34.71 |
| 街区 3 | 37.08 ~ 44.77 | 26.07 ~ 34.71 |
| 街区 4 | 6.20 ~ 13.85 | 11.24 ~ 19.83 |
| 街区 5 | 20.13 ~ 30.79 | 11.24 ~ 19.83 |
| 街区 6 | 37.08 ~ 44.77 | 5.29 ~ 19.83 |

取整误差在 **±0.3dm** 以内，且各块尺寸互相印证（街区高度一致为 8.6dm，
三个上排街区 y 完全对齐），可信。

> ⚠️ **但这是图纸值，不是现场实测值。**
> 真实场地是人工搭建的，必然有公差，边界线本身也有宽度。
> **赛前两边都用卷尺抽验一两块**（建议验起降区尺寸 + 左中街区的 y 下沿这两处），
> 以现场实测为准；发现偏差请回来改本文，别各自在代码里打补丁。

---

## 三、一个共同前提：两边必须标到同一个左下角

本文所有坐标都相对"巡防区左下角"。机端靠 `home_field_x_dm / home_field_y_dm`
把 Cartographer 的 map 系对齐到场地系，车端靠 `arena_origin_map_x_m / y_m / yaw_deg`。
**任一侧标偏，这张表就整体失效。**

标定后交叉验证：把飞机停在某个已知格点，看车端显示的坐标对不对。

（火源坐标本身的精度要求不高——车开到那片区域后还会用自己的摄像头微调。
但**避障用的街区坐标必须准**，那是硬碰硬的。）

---

相关文档：《G题_机车通信接口约定.md》（通信契约）、`src/fire_control_pkg/README.md`（机端任务逻辑）。
