# 给车端 AI 的任务提示词（第 4 版 · 定位失败处理）

> 基于对 `diansai_H_car` commit `204ade1` 的代码复核。
> **第 3 版（seq 复位与触屏按钮）已作废** —— 那一版的 A/B/E/F 在 `204ade1` 里都做完了。
> 本版只剩一个问题，但它会在第一次实车测试时大概率撞上。

====

## 背景

你在维护 2023 电赛 G 题「空地协同智能消防系统」的**消防车侧** ROS 2 Humble 工作区
（仓库 `diansai_H_car`，板子是 Orange Pi 5 Max）。车收到无人机报来的火源坐标后，
BFS 绕开街区开到照射位、激光照射 2.1 秒、返回出发区。

代码已经在板子上构建通过，UDP 四条通道、仪表盘、激光 GPIO、开机自启脚本都已完成。
**下一步是第一次实车测试**，本轮任务是修掉一个会让实车测试直接失败的健壮性问题。

**硬约束**：不要改动任何 UDP 包的字节布局和端口号；不要重构 BFS 规划逻辑。

---

## 唯一任务：`get_pose()` 失败处理过于脆弱

### 问题

`src/follower_pkg/src/fire_mission_manager.cpp` 第 163 行：

```cpp
return inside_arena(point) && std::isfinite(yaw_map);
```

调用方（第 279 行 `on_fire()`、第 339 行 `tick()`）：

```cpp
if (!get_pose(current, yaw)) {
  fail("tf_lost");
  return;
}
```

而 `fail()` 把状态置成 `State::SAFE_STOP`，**这是个终态，不重启节点就永远回不来**。

三个问题叠在一起：

**① 单次 tf lookup 失败就结束整局。**
`tick()` 是周期调用的，Cartographer 偶发一帧查不到 tf 很常见（节点刚启动、急转弯、
雷达丢一帧、CPU 抖动）。只要撞上一次，车就永久停在原地，整场比赛的发挥部分 25 分归零。
一次瞬时的定位查询失败，不应该等同于任务失败。

**② `inside_arena()` 返回 false 也走同一条路径，但 reason 写的是 `tf_lost`。**
这意味着：**场地原点标定稍微偏一点、或者车轮压到边界外一点点，车就整场停摆**，
而你们在屏幕和日志上看到的是「tf 丢了」，会跑去查雷达和 Cartographer——
**根因其实在标定**。这个误导在比赛现场极其致命，因为你只有几分钟排查时间。

注意 `arena_origin_map_x_m / y_m / yaw_deg` 目前还是 `0.0`，要到场地才标。
所以这条在第一次实车测试时**大概率会撞上**。

**③ `SAFE_STOP` 不可恢复。** 哪怕定位下一秒就恢复正常，车也不会继续任务。

### 要求你做的

1. **区分两种失败原因，不要混在一起。**
   把 `get_pose()` 拆成「tf 查询是否成功」和「算出来的点是否在场内」两件事，
   分别对应 `failed:tf_lost` 和 `failed:outside_arena` 两个状态字符串。
   （无人机侧只是把这个字符串显示出来，加新的 `failed:<reason>` 取值不需要机端同步改。）

2. **瞬时失败要容忍。** 连续失败**持续超过一个阈值**（做成参数，默认 1.0 秒）才调用 `fail()`。
   期间：
   - 保持上一次的有效目标点，或者原地停车（你判断哪个更安全，在报告里说明理由）
   - 打 WARN 日志（throttled），说明已经连续失败多久了
   - 一旦恢复，计数清零，任务继续

3. **`outside_arena` 的判据要留余量。** 车轮压到场地边界线外几厘米就整场停摆是不合理的。
   建议：坐标超出场地边界**一定裕量**（做成参数，默认 3dm）才算真的出界；
   在裕量之内只打 WARN 不中止。

4. **`SAFE_STOP` 给一条恢复路径。** 至少提供一个 ROS 话题或服务能把状态复位回 `IDLE`
   （例如 `/fire_mission_reset`），让现场不用重启整个 launch 就能重来一次。
   仪表盘上如果能加一个「复位」按钮更好（触屏可点，和启动按钮同样的做法）。

### 顺带（优先级低，有空再做）

`fire_event_bridge::reply()` 里终态连发 3 次用的是 `std::this_thread::sleep_for(50ms)`，
而它在 `/fire_mission_status` 的订阅回调里执行 —— 单线程执行器会被阻塞约 100ms，
期间 20ms 的 UDP 收包定时器停摆。UDP 有内核缓冲所以实际丢包风险不大，
但改成定时器驱动（像 `fire_link_bridge` 发 CAR_START 那样用 `send_timer_`）更干净。

---

## 交付要求

1. 在 Orange Pi 5 Max 上真正构建并运行，报告里说明每项**实际验证**的方式和结果。
2. 验证方式建议：
   - **瞬时失败容忍**：任务进行中临时 `ros2 lifecycle` 停掉或 kill 掉 Cartographer 不到 1 秒
     再恢复，车必须继续任务而不是永久停摆
   - **持续失败**：让 tf 断开超过阈值，必须报 `failed:tf_lost` 并安全停车
   - **出界**：手动把 `arena_origin_map_x_m` 改成一个明显错误的值，让算出的坐标落到场外，
     必须报 `failed:outside_arena` 而**不是** `failed:tf_lost`
   - **恢复**：触发 `SAFE_STOP` 后用复位接口回到 `IDLE`，再发一次火源坐标能正常出发
3. 不许改动 UDP 包的字节布局和端口号；不许重构 BFS 规划逻辑。

====

---

## 备忘（不用发给队友）

`204ade1` 已经完成的：seq 复位（两个桥，3s，带 INFO 日志）、触屏按钮
（setMouseCallback + 2s 防连点 + 变色反馈）、`done`/`failed` 连发 3 次、
标定文档（方向约定 + 示例数值 + 两点复验）、`init_start_button_gpio.sh`、
`start_fire_mission.sh`（等 X11 + flock + 日志轮转）。

剩下全是真机验证：ping/tcpdump、双机对拉、按键电平、触屏实点、拔电重启验自启、
激光上电、场地三组数值实测、实车不压线。
