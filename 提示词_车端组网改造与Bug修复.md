# 给车端 AI 的任务提示词（第 2 版 · 组网方案变更）

> 用法：把下面 `====` 之间的全部内容复制给队友的 AI 编码助手。
> 它是自包含的（对方冷启动、没有本对话上下文）。
>
> **本版取代上一版（自建热点方案）。** 上一版要求的"从遥测包学地址"等工作**不作废**，
> 只是常量和优先级变了，本版已说明。

====

## 背景

你在维护 2023 电赛 G 题「空地协同智能消防系统」的**消防车侧** ROS 2 Humble 工作区
（仓库 `diansai_H_car`，板子是 Orange Pi 5 Max，Ubuntu 22.04 / aarch64）。

系统由一架无人机和一台消防车组成。无人机全覆盖巡逻找火源、抛灭火包并把火源坐标报给车；
车收到后避开街区开过去、激光照射灭火、返航。两块板 `ROS_DOMAIN_ID` 不同、DDS 不互通，
**所有跨机通信走 UDP**，包格式定义在仓库根目录的《G题_机车通信接口约定.md》。

车端已实现的部分（**不要重构**）：

- `fire_event_bridge` — UDP 8889 收火源包（16B，magic `0xFC11`）→ `/fire_event`；
  并把 `/fire_mission_status` 回传给发送方的 8890 口
- `fire_link_bridge` — UDP 8892 收无人机遥测（32B，magic `0xF14E`，type=1）→ `/drone_telemetry`；
  按键触发时向无人机 8893 发 `CAR_START`（32B，type=3，连发 5 次）
- `fire_mission_manager` — BFS 绕街区规划 → 照射位 → 激光 2.1s → 返航
- `fire_dashboard.py` — 全屏 OpenCV+PIL 仪表盘：场地图、航迹曲线、里程、链路状态
- `laser_gpio_driver` — 激光抽象，当前 `mock_mode: true`

**硬约束：UDP 包的字节格式和端口号一律不许改。** 无人机侧已按现有格式实现并验证过字节布局
（两侧结构体做过逐字段偏移比对），任何改动都要两边同步，成本很高。

---

## 任务 A：组网方案变更（自建热点 → 路由器共享局域网）

### 变更说明

之前让你按「车板用外接 USB 网卡开热点 `AIC800-Hotspot`，车 `10.42.0.1`，
无人机 DHCP 拿 `10.42.0.x`」来做。**该方案已废弃**——车板自建热点实测效果差。

现在改为：**两机接同一台路由器，同网段，两端都是固定 IP。**

| 项 | 值 |
|---|---|
| 网段 | `192.168.10.x` |
| **消防车** | **`192.168.10.113`（固定）** |
| **无人机** | **`192.168.10.197`（固定，已确认）** |

### 要求你做的

1. **`fire_params.yaml` 里 `fire_link_bridge.drone_ip` 填 `192.168.10.197`。**
   这个值已经定死了。如果你之前按上一版填了占位的 `192.168.10.171`，**必须改掉**。

2. **上一版让你做的三级地址解析：保留，不要删。** 只是优先级变了。

   原来是：① 从遥测包 `recvfrom` 学发送方地址（首选）→ ② 静态参数 → ③ 子网广播。

   现在无人机是固定 IP，所以 **② 静态参数成为主路径**。但①和③**必须保留**，
   它们现在的角色是安全网：

   - 万一路由器 DHCP 覆盖了静态配置、或换了板子忘改参数，①能自动救回来
   - ③兜住「参数填错 + 还没收到过遥测」的最坏情况

   保留的代价只是几十行已经写好的代码；不保留的风险是**现场因为一个 IP 填错就整场按键失灵**。
   而 `sendto` 发到不存在的地址**是不会报错的**，这种故障现场极难排查。

   > 如果你判断当前实现的优先级顺序需要调整（比如静态参数应该优先于学到的地址），
   > 自己决定并在报告里说明理由。关键是三条路都在、且日志能看出走了哪条。

3. **广播兜底地址常量** 从 `10.42.0.255` 改成 **`192.168.10.255`**，做成参数。

4. **日志**：每次发 `CAR_START` 时 INFO 打印实际目标 IP 和它的来源
   （`learned` / `param` / `broadcast`），现场一眼能看出发到哪了。

5. **清理**：把仓库里所有 `10.42.0.x`、`AIC800-Hotspot`、`wlx500ff5fbebbb`、
   NetworkManager 热点相关的配置和文档说明**全部删掉或标注废弃**。
   包括 README 里那一大段热点自启/NAT 防火墙的运维步骤——那套现在不适用了。

### 需要你验证并写进 README 的运维项

1. **两机能否互相 ping 通**。路由器如果开了 **AP 隔离 / client isolation**，
   同网段设备之间是不通的——这会让所有 UDP 静默失效。
   **先 ping，再查代码**，这一条写在 README 排查步骤的第一行。
2. **防火墙**：确认车板没有拦截 UDP **8889 / 8892** 入站。给出验证命令。
3. **固定 IP 怎么配**：在 Orange Pi 5 Max 上给这块网卡配 `192.168.10.113` 静态地址的步骤
   （NetworkManager 或路由器侧 DHCP 静态租约，二选一，说明你选了哪个及原因）。
4. **信道**：赛场 2.4G 极度拥挤。README 里提一句：现场若丢包严重，
   优先动**路由器**的信道设置（1/6/11 里挑干净的），别急着改代码。

### 顺带提一句（不用现在做）

本轮比赛之后计划把这条链路**改成蓝牙**。所以请**不要**把 UDP 收发逻辑散进
`fire_mission_manager` 或仪表盘里——保持现在"传输层集中在桥节点、
对外只暴露 ROS 话题"的结构，将来换传输层才能只动桥节点。
如果你发现现有代码有违反这一点的地方，在报告里指出来。

---

## 任务 B：修 4 个已确认的 bug

按严重程度排序。**这部分与上一版完全相同，如果你已经修过就跳过并在报告里说明。**

### B1 🔴 `fire_dashboard.py` 启动即崩（最高优先级）

```python
self.declare_parameter("obstacles_dm", [])
```

rclpy 从默认值推断参数类型。`all()` 对空列表恒为 `True`，导致空列表被推断成
**`BYTE_ARRAY`**；而 YAML 给的是 double 数组，Humble 的静态参数类型检查直接抛：

```
InvalidParameterTypeException: Trying to set parameter 'obstacles_dm' to
'[6.0, 24.0, ...]' of type 'DOUBLE_ARRAY', expecting type 'BYTE_ARRAY'
```

**节点根本起不来**，仪表盘一片空白 = 直接丢 16 分。这已经在 rclpy 上实测复现。

修法（已验证）：

```python
from rclpy.parameter import Parameter
...
self.declare_parameter("obstacles_dm", Parameter.Type.DOUBLE_ARRAY)
```

声明类型而不给默认值，YAML 覆盖正常生效；YAML 万一没加载上会**当场报错**而不是
安静地画一张没有街区的图。

同时请检查该文件其它 `declare_parameter` 有没有同类问题（空列表 / `None` 默认值）。

### B2 🟠 中文可能全变方块

`_find_cjk_font()` 的候选列表兜底到 `DejaVuSans.ttf`，**它没有任何中文字形**；
再兜底的 `ImageFont.load_default()` 同样没有。板子上若没装中文字体，
仪表盘上「位置/高度/阶段/里程/链路/火源」全部渲染成豆腐块。

要求：

- 找不到 CJK 字体时**在日志里 WARN 明确报出来**，不要静默降级到 DejaVu
- README 里写明依赖：`sudo apt install -y fonts-noto-cjk`，
  并给出自检命令 `fc-list :lang=zh | head`

### B3 🟡 目前只能靠键盘启动

`button_gpio_value_path` 默认空，启动只能按仪表盘的空格/回车 —— 意味着比赛时车上得插键盘。
题目原文是「在消防车上按键启动」，物理按钮稳妥得多，而代码已经支持
（sysfs GPIO 路径 + 去抖），只差接线和填参数。

要求：在 README 里补一节，写清楚怎么在 Orange Pi 5 Max 上把某个 40-pin 引脚配成输入、
怎么导出 sysfs、`button_gpio_value_path` 该填什么、`button_active_low` 怎么定。
不需要你接线，把文档写到照着做就能通即可。

### B4 🟡 `stamp_ms` 塞进 `Float32MultiArray` 掉精度

`/drone_telemetry` 把 uint32 的 `stamp_ms` 转成 float32，只有约 7 位有效数字，
毫秒时间戳会被舍入到几百 ms 粒度。目前仪表盘没用它所以无害，但接口上是坑。

要求：要么从消息里删掉这个字段（同步改 `layout.dim[0].label` 和 size），
要么在代码和文档里注明「仅供调试，精度不足以做延迟测量」。二选一，说明你选了哪个及原因。

---

## 交付要求

1. **必须在 Orange Pi 5 Max 上真正执行 `colcon build` 并跑一次 `ros2 launch car_launch fire_mission.launch.py`。**
   这个仓库至今一次都没在板子上编译过 —— B1 那个 bug 只要跑一次就会暴露，
   说明之前确实没跑。不要只做静态检查就说完成。
2. 报告里逐条说明每项改动、以及你**实际验证**的方式和结果。
   没验证的就明说「未验证」，不要含糊带过。
3. 不要改动任何 UDP 包的字节布局和端口号。
4. 不要重构 `fire_mission_manager` 的规划逻辑，本次不涉及。

## 明确不在本次范围内（但请在报告末尾提醒一次）

这三项不做，发挥部分 3+4 的 25 分依然是零：

- `laser_gpio_driver` 仍是 `mock_mode: true`，激光不会亮
- `obstacles_dm` 六个街区矩形是照赛题图估的，**必须现场实测**
- `arena_origin_map_x_m / y_m / yaw_deg` 全是 0，**场地原点没标定**，
  不标的话收到火源坐标也会开到错地方

====

---

## 无人机侧状态（不用给队友，我自己的备忘）

已全部改完并与本文档一致：

- `fire_link_pkg`：`car_ip` = `192.168.10.113`；火源上报 16B `0xFC11` → 8889；
  遥测 32B → 8892；启动包接收 8893；车端状态字符串接收 8890 → `/car_status`
- 包头两侧结构体逐字段偏移比对通过（`g++` 交叉 `static_assert`）
- 《G题_机车通信接口约定.md》§一 已更新为路由器拓扑
- `scripts/fire_link_test.py` 默认端口/IP 已对齐

待办：无人机固定 IP 定下来后，同步填进接口约定文档和车端 `drone_ip` 参数。
