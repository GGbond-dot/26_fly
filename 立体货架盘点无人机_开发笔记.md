# 立体货架盘点无人机（D 题）开发笔记

> 2024 电赛 D 题《立体货架盘点无人机系统》。本分支 `inventory-task` 从 **`spray-task`** 切出
> （底座含 G题 spray 全套代码），复用本仓现有飞控链路（PID / uart_to_stm32 / cartographer），
> 只新增"二维码识别 + 激光指示 + 盘点任务状态机"。旧版 demo 见 24fly 仓库（GGbond-dot/24fly），
> 核心识别代码已移植本仓。
>
> **为什么基于 spray-task**：spray-task 上的 `spray_control_pkg/spray_mission_node` 是最新、最完整的
> "起飞→航点→到点干活→降落"任务状态机样板，D题 的 `inventory_mission_node` 直接仿它写，省得重造；
> 同时 spray-task 已适配的飞控/相机硬件本分支全继承。

---

## 0. 一句话方案

单相机 + 空中 yaw 旋转扫面（教练组 2026-05-29 定）：一个相机水平朝前，飞到一个货架面，
逐货位对准→激光点亮 0.5s→读编号→记录；扫完一面整机 yaw 转 180° 扫背面；两个货架四个面共 24 码。
**双相机是后期赶时间的备选**，先把单相机 + yaw 旋转跑通。

---

## 1. 题目要点（评分锚点）

场地 400×500cm，黑方块=起飞点，黑圆=降落点（二者不同位置）。2 个双面货架，每面 6 个货位
（2 行 × 3 列，列距 50cm，行间 40+40cm，二维码 19×19cm，内容为数字 1~24）。

| 要求 | 关键动作 | 分 | 限时 |
|---|---|---|---|
| 1-1 | 垂直起飞到 150±10cm | 5 | — |
| 1-2① | 遍历 24 码，**激光在码范围内点亮 0.5s** | 24 | 270s |
| 1-2② | 地面站 LCD 实时显示 编号+货位(A1~D6)，每盘一个 **LED 亮灭 1s** | 12 | |
| 1-3 | 稳定降落在降落点，几何中心不出界 | 5 | |
| 1-4 | 盘点后地面站显示全部结果，可输入编号查货位 | 5 | |
| 1-5 | 遍历越快越好 | 10 | |
| 2 | 定向盘点：先识别 1 张抽取码报送编号→显示规划航线→飞过去盘点→降落 | 34 | 180s |

硬件硬约束：轴距 ≤45cm；桨全防护；**激光水平安装、不可动**、光斑 ≤6mm；一键启动后全程不得人为干预、不得中途降落。

---

## 2. 复用 vs 新写（与 24fly 的关系）

24fly 和本仓 26fly 同源，底层 `activity_control_pkg / pid_control_pkg / uart_to_stm32 /
serial_comm / my_carto_pkg / bluesea2` 共享。**26fly 的这些包是进化版（适配 humble + 双USB相机 +
面阵激光的新硬件），不要用旧包覆盖。** D 题只把 24fly 里独有的二维码逻辑搬过来：

| 模块 | 来源 | 处理 |
|---|---|---|
| 二维码识别 + 激光脉冲 + 对准 | 24fly `opencv01/decoder_common.py` | ✅ 已移植为 `qr_vision_pkg`，**改单相机**、加 `rotate_code`、加 `/qr_vision/enable` 门控 |
| 偏移→机体系微调 | 24fly `opencv01/qr_fine_tune.py` | ✅ 已移植，默认前缀改 `/qr_vision` |
| 航点 + 视觉对准切换 | 24fly `activity_control_pkg` 的 `/current_target_camera` 左右切换 | ❌ 不直接用——改成单相机 yaw 航点，逻辑并入新任务节点 |
| 盘点任务状态机 | 新写 | ✅ `inventory_control_pkg/inventory_mission_node`（仿 spray 骨架） |
| 飞控链路 PID/uart/carto | 26fly 现有 | 直接复用，不改 |
| 地面站 LCD/LED + 编号查询 | 新写（待做） | ⏳ 见 §6 |

---

## 3. 本分支已建骨架（待另一台机 colcon build）

### `src/qr_vision_pkg`（ament_python，移植自 opencv01）
- `qr_vision_pkg/decoder_common.py` → 节点 `qr_vision`：单相机识别。
  发布 `/qr_vision/id`(String)、`/qr_vision/offset_norm`(Point)、`/qr_vision/aligned`(Bool)、
  `/qr_vision/debug_image`。订阅 `/qr_vision/enable`(Bool) 总开关。
  识别到码且横向进窗、且与上次不同码 → 子线程用香橙派 WiringOP `gpio write` 把 `laser_pin`
  拉到 on_level 0.5s 打激光（命令行控脚，非 python wiringpi 模块；见 §5-3，实测 pin=13）。
  关键参数：`camera_device / rotate_code(下视=2) / fourcc=MJPG / laser_pin(=13) /
  laser_on_level(0)/laser_off_level(1) / use_electromagnet(默认False) / eps_x / eps_x_laser /
  stable_frames / decode_interval`。相机用 `cv2.CAP_V4L2` 后端打开。
- `qr_vision_pkg/qr_fine_tune.py` → 节点 `qr_fine_tune`：偏移→机体系 cm 微调（死区+EMA+限步）。

### `src/inventory_control_pkg`（ament_cmake，新写）
- `inventory_mission_node`：盘点任务状态机。
  - 阶段：`IDENTIFY`(仅定向) → `TAKEOFF` → `TRAVERSE` → `RETURN` → `LAND` → `DONE`
  - 航点 `InventoryWaypoint{x,y,z,yaw,scan,slot,tag}`；scan 航点到位悬停开识别，等"对准+读码"。
  - 发布：`/target_position`、`/active_controller(2)`、`/route_choice(1)`、`/qr_vision/enable`、
    `/inventory_result`(String 编号+货位→LCD)、`/inventory_led`(Empty→LED)、
    `/inventory_target`(String：先报抽取码编号"7"，地面站下发货位后再报"目标编号=7,货位=C5")、
    `/mission_complete`。
  - 订阅：`/height`、`/qr_vision/id`、`/qr_vision/aligned`、
    **`/inventory_target_slot`(String "C5"：要求2 地面站查表后下发货位)**；位姿走 tf `map→laser_link`。
  - `slotToScanWaypoint(slot)`：货位串→航点，遍历/定向**共用**（保证定向直飞点==遍历记录点）；
    **无镜像**：四面统一 `col=(idx-1)%3` 按飞机观测 y 递增取列、`row=(idx-1)/3` 取行（详见下）。
- `launch/inventory_mission.launch.py`：起 qr_vision + qr_fine_tune + 任务节点。
  `mode:=traverse|directed`。**PID/uart/carto 用本仓现有 launch 另起。**

---

## 4. 任务流（状态机）

**WAIT_MODE（上电待命，2026-06-05 新增）**——飞机重启后**先原地待命、不起飞、不接管控制器**，
等地面站下发 `/inventory_mode`(`traverse`/`directed`) 告知本轮跑普通还是进阶，收到才 `configureForMode()`
建航线并起飞。**一轮比赛飞机会重启(要求1飞完换电池切要求2)、地面站全程不重启**，重启后的飞机不知道
该跑哪个任务 → 靠地面站「普通任务/进阶任务」大按钮告知。顺带根治 autostart 上电即起飞的安全隐患。
（rotate_test 调试模式不走 WAIT_MODE，仍按 launch 参数立即起飞。）

**遍历盘点（要求1）**
```
WAIT_MODE → (地面站发 traverse) →
TAKEOFF(home,150) → [A面 A1..A6] → yaw180 → [B面 B1..B6]
                  → 飞货架2 → [C面 C1..C6] → yaw180 → [D面 D1..D6]
                  → RETURN(降落点上空,150) → LAND(降落点)
```
每个 scan 航点：到位 → settle 0.8s → `/qr_vision/enable=true` → 等 `aligned && id`
→ 记录 `slot→编号` → 发 `/inventory_result` + `/inventory_led` → 关识别 → 下一个。
超时 `scan_timeout_sec`(默认6s) 跳过，避免卡死整套。

**定向盘点（要求2）**——要求1/要求2 是两次独立飞行（**飞机重启、地面站不重启**），"编号→货位"表权威方=地面站。
```
WAIT_MODE → (地面站发 directed) →
IDENTIFY: 地面读抽取码 → 发 /inventory_target(编号) 报地面站
          → 等地面站查表下发 /inventory_target_slot(货位"C5")
  → TAKEOFF → 直飞该货位盘点(slotToScanWaypoint 映射) → RETURN → LAND
```
飞机无状态、不落盘、不自查表（重启无所谓）。地面站只下发货位串、不下发坐标，
坐标固定由 slotToScanWaypoint 给标称值，细微偏差靠 qr_fine_tune 相机微调。
要求2-2 的"规划航线图"由地面站据货位自行绘制（8分）。

---

## 5. 待标定 / 待决策（动手前必看）

1. **场地坐标系（货架1 已标定 2026-06-05；货架2 C/D 待标）**：坐标原点=起飞黑方框中心 (0,0)，蟹行。
   实测值已写死进 `inventory_mission_node.cpp` 的 `faceGeometry()`（每面查表，非公式，因实测 y 间距非整 50）：
   - **A 面**：飞机 x=30、yaw 0(朝+x)，三列 y=70/123/173，高行 z=129cm、低行 z=90cm。
   - **B 面**：飞机 x=120、yaw 180(原地转)，三列 y=75/128/178，高行 z=133cm、低行 z=91cm。
     （2026-06-05：A/B 飞机 x 由 0/150 各往板面挪 30cm → 30/120，缩小 standoff、离板更近。）
   - 终点（本轮单货架测试）：在最后一面正前方 y 轴 `(120,0)` 降落（随 B 面 x 改 150→120）。正式四面赛降落点应为黑圆 `land_x/y`。
   - **C 面**：飞机 x=230、yaw 0(朝+x)，三列 y=71/123/174，高行 z=132cm、低行 z=90cm。
   - **D 面**：飞机 x=320、yaw 180，三列 y=75/127/178，高行 z=132cm、低行 z=91cm。
     （2026-06-05：货架2 板≈x275，C/D 实测填入 `faceGeometry`，`measured=true`；
     实飞全打中后 D 面三列 y 整体 +4 → 75/127/178。）
     **D 末两码单货位 y 微调（2026-06-06 实飞复核）**：靠 `slotYAdjustCm()` 单独覆盖，不动同列另一货位。
     遍历 D 面扫描序 D4,D5,D6,D3,D2,D1 → 倒数第二=D2、倒数第一=D1。**D2 -6（同列 D5 不动）、D1 -6**
     （D1 原 -3，本轮再减 3 → -6；同列 D4 不动）。个别货位偏差走这个单货位微调函数，不动整列。
   - **黑圆降落点 (350,250)**（实测）：四面全跑落此处，靠任务节点 `land_at_circle=true` + `land_x/y_cm` 开关。
   - 货架间过渡：扫完 B(x120,yaw180) 沿 +x 飞到货架2 前悬停点(x230) 原地转 yaw0，C 面照搬 A/B 蛇形（高度几乎相同）。
     现有 `buildTraverseWaypoints` 的「换面三步(回过渡y→平移x→原地转)」即此逻辑，x230 就是悬停旋转点。
   - **换面过渡 y = -20**（参数 `transit_y_cm`，默认 -20）：换面三步(回收/平移/旋转)退到 y=-20 脱离板面
     （不再贴 y=0），避免平移时蹭到货架。scan 点仍是各列实测 y(70~178)，单货架测试落点仍 y=0。
     落黑圆前不退 y 轴（末面 D 收尾已在货架2 外侧，平移到黑圆不会撞板），D 收尾后直接平移到黑圆降落。
   `buildTraverseWaypoints()` 走**蛇形（弓字形）**：面间 A 先高行/B 先低行（上一面低行收尾→下一面低行起步，
   省上下折返）；面内第一行 y 递增、第二行 y 递减；换面三步（回 y 轴→沿 x 平移→原地旋转）。
   ✅ 货位号↔码对应**已确认（2026-06-05，四面统一、无镜像）**：上排=1/2/3(高z)、下排=4/5/6(低z)；
   同排按**飞机观测 y**：y 小→1,4 / y 中→2,5 / y 大→3,6（`row=(idx-1)/3`、`col=(idx-1)%3` 取 y 递增列）。
   B/D 背面虽转 180°，但货位号按飞机观测 y 算（非相机画面左右），故同一规律对背面成立，无需镜像/翻转。
   **飞机观测坐标已是实测值**，遍历/定向共用 `slotToScanWaypoint` 落点一致。launch 默认 `traverse_faces=A,B,C,D`。
2. **相机方位（已定 2026-06-04）**：盘点相机=植保下视 down_cam(`/dev/video0` by-path)，画面
   **逆时针90°转正 → `rotate_code=2`**（同前视条码相机）。`qr_fine_tune` 的 `invert_body_*`
   符号仍待标（决定"看到码偏左/上时往哪飞"）。相机用 `cv2.CAP_V4L2` 后端打开（否则 GStreamer 刷警告）。
3. **激光 GPIO（已定 2026-06-04）**：走香橙派 **WiringOP `gpio` 命令行**（同 magnet_control_pkg，
   *不是* python wiringpi 模块——那个没装）。**实测 `laser_pin=13`、on=低(0亮/1灭)**，`gpio write 13 0/1`
   无需 sudo 即有反应。植保那条 `/electromagnet_control`(STM32 0x33) 链路实测不稳已弃用
   （decoder 留 `use_electromagnet` 开关默认 False）。激光水平安装，靠飞机移动让光斑落到码上，
   识别端 `eps_x_laser` 控制"何时认为对上了才打"。
4. **编号→货位映射（定向盘点）——已定型（2026-06-02）**：
   读真题确认要求1、要求2 是**两次独立飞行/测试**（说明3-4：两次测试间允许换电池），且
   **要求2-2（8分）要求起飞前地面站就显示规划航线图** → 飞机必须起飞前就知道货位。
   "边飞边读搜索"方案作废（拿不到2-2 那8分且慢）。
   **定方案：查表，且表的权威方=地面站**（队友实现，飞机这边重启无状态）：
   - 要求1 遍历时飞机逐货位发 `/inventory_result`，地面站据此建"编号→货位"全表（要求1-4 本就需要）。
   - 要求2 飞机地面读抽取码 → 发 `/inventory_target`(编号) → **地面站查表 → 下发货位**
     `/inventory_target_slot`(String "C5") → 飞机 `slotToScanWaypoint("C5")` 映射成航点直飞。
   - 飞机**不落盘、不自查表**（重启无所谓，地面站不跟飞机一起重启）。
   - **"地面站不跟飞机一起重启"的含义（2026-06-05 厘清）**：一轮比赛里飞机会重启(要求1飞完换电池切要求2)，
     地面站全程不重启，"编号→货位"表活在地面站**内存**里供要求2 查 → 不靠落盘跨重启重载。所以：
     ①飞机重启后不知道本轮跑哪个任务 → 地面站「普通任务/进阶任务」大按钮发 `/inventory_mode` 告知（飞机 WAIT_MODE 等它，见 §4）；
     ②**地面站自身重启=开新一轮 → 一律清空**(`MainWindow.__init__` 调 `clear_results()`)，避免上轮旧结果残留误导
     （`InventoryStore` 类的落盘/load 能力保留给单测，但 app 启动不再 load 旧 json）。
   - 货位坐标固定，地面站只给货位字符串、不给坐标；细微偏差由 `qr_fine_tune` 相机微调。
   - `slotToScanWaypoint()` 遍历/定向**共用**，保证"定向直飞的点"=="遍历记录该货位的点"。
   - ⚠️待和队友对接的接口：地面站需发布 `/inventory_target_slot`(String, 如 "C5")。
5. **yaw 旋转的飞控配合**：单相机靠整机 yaw 转 180° 扫背面，确认 PID 的 yaw 通道能稳住转向+悬停。

---

## 6. 地面站（LCD/LED + 编号查询）— 已落地（队友实现，2026-06-02 并入仓库）

队友的 **PyQt5 + rclpy(humble)** 地面站已拷进仓库 `diansai_terminal/`（独立上位机节点，
非 STM32 侧屏；跑在另一块香橙派上，全屏运行）。无 git 历史。

### 飞机状态/心跳栏（2026-06-06 新增）——比赛时只能看地面站
**背景**：比赛现场操作员**不能远程登录飞机**（cv2 预览窗只在飞机屏上，平时调试可远程/VNC 看，比赛看不到）→
飞机所有状态必须走地面站显示。两个痛点：①飞机重启后怎么知道它**起好了在待命**；②点了进阶后**识别没识别到抽取码**。
- **飞机端**：`inventory_mission_node` 每 ~0.5s 发一条 `/inventory_status`(String) 当心跳+状态
  （`buildStatusText()`/`publishStatus()`，在 `monitorTimerCallback` 顶部、WAIT_MODE 早退前就发，
  所以待命态也持续跳）。文本随阶段变：`待命中…`/`[定向]识别抽取码中…`/`[定向]已识别抽取码 编号N，等货位`/
  `[遍历]盘点中 货位A3`/`返航中`/`任务完成` 等。
- **地面站顶栏新增两栏**（main.py）：`uav_link_label`=「飞机:在线/离线」（靠心跳时间戳判，>2.5s无心跳=离线变红，
  区别于 `ros_state_label` 那个只表地面站自身 rclpy 连接）；`uav_phase_label`=「状态:<飞机心跳文本>」。
  `handle_uav_status` 记时间戳，`_refresh_uav_link`（ui_timer 500ms 周期）判在线/离线+着色。
- **识别反馈**：`handle_target_id` 收到 `/inventory_target` 编号即在 query_result_label 显
  `✅已识别抽取码：编号N → 库位XY，正在下发…`（不在表里则提示「不在本轮遍历结果里」）。
- `fake_uav` 同步发 `/inventory_status`(~2Hz timer)，脱机联调能看到在线灯+阶段变化。
- ⏳ 待两机联调验证心跳在线判定 + 各阶段文案。

### 任务模式按钮 + 重启清空（2026-06-05 新增）
- **左侧顶部两个大按钮「普通任务(要求1遍历) / 进阶任务(要求2定向)」**(`#modeButton`, 高104/字号30)，
  点了 `send_mission_mode()` → `bridge.publish_mode()` 发 `/inventory_mode`(traverse/directed) 给待命飞机
  （飞机 WAIT_MODE 等这条才起飞，§4）。下方 `mode_status_label` 显示已下发的模式。
- **地面站启动一律清空**：`MainWindow.__init__` 创建 store 后调 `clear_results()`，每次启动从干净态开始
  （一轮里地面站不重启，要求2 靠内存表；启动=开新一轮，见 §5-4）。
- `fake_uav` 同步：先等 `/inventory_mode` 再动作，`directed` 走 `_run_directed`(只报抽取码+等货位)、
  `traverse` 跑全 24 槽。脱机联调要求2 需先 traverse 建表(地面站内存)、保持地面站运行、再发 directed。

### 目录
- `ground_station/models.py`：数据层。`SLOTS`(A1~D6=24)、`Waypoint`、`InventoryStore`
  (结果落盘 `data/inventory_state.json`、`find_item`/`item_to_slot` 编号↔货位互查)；
  解析飞机字符串的 `parse_inventory_payload` / `parse_target_payload`。
- `ground_station/ros_bridge.py`：ROS 桥，独立线程跑 rclpy，Qt signal 转发。
- `ground_station/main.py`：UI。仓库俯视图(货架/起降点/24槽位红=未盘/绿=已盘/橙=异常/横平竖直航线)
  + 盘点表格 + 编号查询/发库位/清空 + LED 指示灯。
- `ground_station/fake_uav.py`：模拟无人机，按真实话题/格式发 24 个结果→mission_complete→目标编号，
  收货位后回确认。无飞机时脱机联调地面站用。
- `config/slot_waypoints.json`：场地 + 24 槽位坐标（**占位值，⚠️见下方坐标系警告**）。
- `tests/test_models.py`：数据层单测（含飞机格式解析），`python3 -m unittest discover -s tests` 7/7 过。

### 话题契约（已统一，地面站去贴飞机话题；无 `/uav` 前缀）
原队友版用 `/uav/*` + JSON/Int32，与飞机对不上 → 已**改地面站**对齐飞机现有话题：

| 方向 | 话题 | 类型 | 内容 |
|---|---|---|---|
| 飞机→地面站 | `/inventory_result` | String | `编号=7,货位=B3`（遍历逐货位上报，要求1-2②/1-4 建表）|
| 飞机→地面站 | `/inventory_target` | String | 先 `7`（报抽取码编号）；地面站下发货位后回 `目标编号=7,货位=C5`（确认）|
| 飞机→地面站 | `/inventory_led` | Empty | 每盘点一个，LED 亮灭一次（要求1-2②）|
| 飞机→地面站 | `/mission_complete` | Empty | 本轮任务完成 |
| 飞机→地面站 | `/inventory_status` | String | **状态/心跳(~0.5s一条)**：待命/识别抽取码/已识别编号N/盘点货位X/返航…地面站顶栏显示「飞机在线+阶段」(2026-06-06)|
| 地面站→飞机 | `/inventory_target_slot` | String | `C5`（定向盘点查表后下发货位，要求2）|
| 地面站→飞机 | `/inventory_mode` | String | `traverse`/`directed`（飞机重启后告知本轮普通/进阶，飞机在 WAIT_MODE 等这条才起飞）|

LED 由 `/inventory_led` 单独驱动（不再靠结果 ok 推断）；定向确认回报只更新显示、不重发。

### 地面站物理 LED 灯（2026-06-07 新增）——和飞机激光同一套 GPIO 接口
要求 1-2② 每盘点一个 LED 亮灭 1s。原来只有屏幕上一个 LED 方块闪（保留，好看），现额外驱动一颗
**物理 LED 灯**，接法/控制方式**和飞机激光完全一致**（香橙派 WiringOP `gpio` 命令控脚，见 §5-3）：
`gpio mode <pin> out` + `gpio write <pin> <level>`，**on_level=0 亮 / off_level=1 灭，默认 pin=13**（同激光）。
- **文件**：`ground_station/gpio_led.py`（`GpioLed` 类，照搬 `decoder_common.py` 激光逻辑，子线程亮灭不阻塞 Qt）。
  `main.py` `__init__` 建 `self.led_gpio`，`blink_led()` 收到 `/inventory_led` 时屏幕方块 + 物理灯**同时**亮灭 1s，
  `closeEvent` 退出灭灯。`gpio` 命令找不到（没装 WiringOP）/pin=-1 时**静默降级只闪屏**，不崩。
- **链路**：`fake_uav 或飞机发 /inventory_led → ros_bridge 转 led_blink 信号 → main.blink_led() 驱动 GPIO`
  （物理灯由 main 点，不是 fake_uav 直接点）。
- **CLI 参数**：`--led-pin`(默认13，-1关)、`--led-on-level`(默认0)、`--led-off-level`(默认1)，换脚/电平极性反了不改代码。
- **地面站开发板要装的环境**（同飞机香橙派）：①装 **WiringOP**（`git clone orangepi-xunlong/wiringOP` + `sudo ./build`，
  `gpio readall` 验证）；②**免 sudo**：`sudo usermod -aG gpio $USER` 后**重新登录**；③接线 LED 到 WiringOP pin13（默认低电平亮）。
- **自测脚本**：`ground_station/gpio_led_test.py`（`python3 -m ground_station.gpio_led_test`，不连 ROS 直接闪灯验证
  接线/权限）；`--count/--pin/--on-level/--off-level` 可调。找不到 gpio / 权限失败都有明确提示。
- **端到端联调**：`main` + `fake_uav` 两个独立终端都开着 → 屏幕点「普通任务」→ fake_uav 逐货位发 `/inventory_led` → 灯连闪。
  ⚠**坑（2026-06-07 实测踩过）**：①`fake_uav` 收到 `/inventory_mode` 后 ~3.6s 内一炮把 24 个发完且只认第一次 mode →
  **先起 main 等「ROS 已连接」、再起 fake_uav、最后点按钮**，否则打空炮；②DDS 脚本**必须 `source` 不能 `./`**（见 §7）。

### 「查询坐标」按钮 ≠ 定向盘点（两套独立机制，别混）— 2026-06-03 厘清
真题白纸黑字是两个不同得分项，地面站里也是两段独立代码，**演示时都要有**：
- **要求1-4（5分）「可输入货物编号查询」**：操作员**手动**在输入框敲编号 1~24 → 显示库位/坐标。
  代码 `query_item()`。这是手动查询，**不能改成自动**，否则 1-4 的演示点没了。
- **要求2（定向盘点）**：飞机识别抽取码 → 发 `/inventory_target`(编号) → 地面站**自动**查表、填编号、
  选中库位、地图画航线、下发 `/inventory_target_slot`。代码 `handle_target_id()`，全自动、不用点查询。

### 触摸屏适配 + UI 放大 — 2026-06-03
地面站跑在**触摸屏**香橙派上、无物理键盘、系统也没装虚拟键盘 → 原 `QLineEdit` 点了不弹键盘没法输入。
- **内置悬浮数字键盘**（不依赖系统输入法）：`main.py` 新增 `TouchLineEdit`(只读+NoFocus，点击发
  `clicked`) 和 `NumericKeypad`(无边框居中弹窗，大按钮 1~9/0/←/清 + 取消/确定，校验 1~24)。
  点输入框 → 弹键盘 → 戳数字 → 确定即自动查询(`open_keypad()`)。样式 `KEYPAD_STYLE`。
- **UI 放大**（触屏好按）：左侧三个主按钮(查询坐标/发送库位/清空本轮)字号 28/高 78、`#controlButton`；
  搜索框字号 26/高 62；LED 指示灯 104×52/字号 24；标签页字号 23；左侧面板加宽 300~380。
  地图区(`WarehouseMap`)不动。

### ⚠️ 坐标系警告
`diansai_terminal/config/slot_waypoints.json` 的坐标 **和飞机 `slotToScanWaypoint()` 是两套独立坐标**：
地面站只用它画航线图，飞机自己算航点。两边占位值都还没标定，回实验室标定时**必须对齐到同一
cartographer 地图坐标系**，否则地面站画的图和飞机实飞对不上。

---

## 7. 启动（实飞，待另一台机编译后）

```bash
# 飞机端：每个终端先 source DDS（与地面站同 ROS_DOMAIN_ID）。autostart_fly.sh 已自动 source。
source scripts/setup_dds.sh

# 飞控链路（复用现有，与搬运/植保同款）：carto + uart + 位置PID
# ros2 launch ...（按本仓现有 total/demo launch）

# 盘点任务（仅视觉+任务节点，飞控底座需另起）
ros2 launch inventory_control_pkg inventory_mission.launch.py                 # 遍历
ros2 launch inventory_control_pkg inventory_mission.launch.py mode:=directed  # 定向

# ★要求1 整套基础部分（一把起：飞控底座 + 视觉 + 任务，A,B,C,D 四面 + 落黑圆 350,250）
#   放 my_launch（统一总启动位置，与 rotate_test_basic/demo_basic/spray_basic 并列）
ros2 launch my_launch inventory_full.launch.py
#   只测货架1：             ros2 launch my_launch inventory_full.launch.py traverse_faces:=A,B
#   更柔和转速：           ros2 launch my_launch inventory_full.launch.py max_angular_velocity:=20.0
```

### 开机自启（`scripts/autostart_fly.sh` + `scripts/stop_fly.sh`）
**默认已切到 `my_launch/inventory_full`**（2026-06-05，原为 `inventory_control_pkg/qr_laser_test`）。
上电起完整飞控后**先原地待命 WAIT_MODE 不起飞**，等地面站点「普通任务/进阶任务」按钮下发 `/inventory_mode`
才起飞（§4）——既让重启后的飞机知道本轮跑要求1 还是要求2，也避免上电乱起飞。现场仍须能随时接管。
- 改 launch 名要**同时**改 autostart_fly.sh 和 stop_fly.sh 的 `LAUNCH_PKG/LAUNCH_FILE` 默认值（否则
  stop 的 pkill 匹配不到停不掉）。临时退回纯调试不飞：
  `AUTOSTART_PKG=inventory_control_pkg AUTOSTART_LAUNCH=qr_laser_test.launch.py ./scripts/autostart_fly.sh`
- **录包话题**（产物在 `~/fly_logs/bag_<ts>/`，zstd 文件级压缩）排查重点：
  - `/qr_vision/id`＝**摄像头有没有识别到**（解到码才发，内容编号 1~24）；
    `/qr_vision/offset_norm`+`/aligned`＝对准过程。
  - `/qr_vision/laser_fired`＝**激光有没有打**（打满 0.5s 后回报刚打的码，§10）；
    `/qr_vision/strict_vertical`＝换行升/降时纵向严判是否开。
    ⚠ 激光是香橙派 GPIO 直驱（`gpio write 13`）**不经话题**，bag 里看不到电平本身，靠 `laser_fired` 判打没打。
  - `/target_velocity`(0x31)+`/velocity_map`(0x32)＝复核 yaw180 带偏航平移的机体系旋转方向（§9.3）。
  - 其余：`/inventory_result /inventory_led /inventory_target /inventory_target_slot /standoff/distance
    /height /target_position /mission_step`。
  - `/qr_vision/debug_image`（相机画面）默认不发（launch 没开 `enable_debug_image`），故未录；
    要看相机画面得开该参数再录，但整段视频流即使压缩也很大，不建议常开。

### 地面站（另一块香橙派）
```bash
cd diansai_terminal
source /opt/ros/humble/setup.bash
source scripts/setup_dds.sh        # 与飞机同 ROS_DOMAIN_ID
python3 -m ground_station.main
# 脱机联调：另开终端 source 同款 DDS 后 python3 -m ground_station.fake_uav --target-id 7
```

### 跨机 DDS（飞机 `scripts/setup_dds.sh` + 地面站 `diansai_terminal/scripts/setup_dds.sh`）
两端核心就三行、必须一致：`ROS_DOMAIN_ID=26` + `ROS_LOCALHOST_ONLY=0` +
`RMW_IMPLEMENTATION=rmw_fastrtps_cpp`（统一 FastDDS）。脚本只导出环境变量、不 source ROS。
⚠**必须 `source scripts/setup_dds.sh`，不能 `./scripts/setup_dds.sh`**（2026-06-07 踩过半天）：脚本全是
`export`，带 `./` 跑会开子shell、变量退出即失效 → 当前终端 `ROS_DOMAIN_ID` 没设上、各终端落在不同域、
收发不到。**每个终端开头 source 后 `echo $ROS_DOMAIN_ID` 必须打印 26** 才算对（空/0=没设上）。
现场多网口（docker0/有线/无线）发现不稳时：`NET_IFACE=wlan0 source scripts/setup_dds.sh`
把 FastDDS 按网卡 IP 白名单绑定（默认不启用，纯应急逃生口）。

---

## 8. 板面距离测量（standoff）—— 像素误差转真实 cm

**对准方式：螃蟹式横走**——相机/指示激光/雷达全部正前方安装，机头始终顶着板面，靠
**机体 y(左右平移)**对准货位、`body_z`(高度)对行、`body_x`(前后到板距离)保持 standoff。
所以正确的轴映射是：
- `ex`(横向像素偏移) → **`body_y`**(横移)；
- `ey`(纵向) → `body_z`(高度)；
- `body_x`(前后) **不归像素管，由雷达 standoff 闭环保持**。

✅ `qr_fine_tune` 已按此重写（2026-06-02）：`ex→body_y`、`ey→body_z`、`body_x` 恒输出 0。
（原默认 `k_body_x_cm=20`/`k_body_y_cm=0` 把横向当前后微调，和蟹行对不上，已废弃。）

同样的像素偏移，飞机离板面越远对应的真实 cm 越大，所以增益**随 standoff 距离线性变化**。
decoder 偏移按半宽归一化（`ex=(cx-img_cx)/img_cx`，范围 −1~1），换算关系：

```
真实横向偏移 = ex × D × tan(HFOV/2)        # D = 相机到板面距离(米)
=> k_body_y_cm = D(米) × tan(HFOV/2) × 100  # 横移轴；纵向同理 VFOV -> k_body_z_cm
```

`qr_fine_tune` 已订阅 `/standoff/distance`+`/standoff/valid` 动态算 `k_body_y/z_cm`；
standoff 无效/过期(`standoff_timeout_sec`)/超合理范围时回退静态默认增益。参数 `hfov_deg`/`vfov_deg`
**待标定**（launch 里占位 60°/37°）。

测这个 D 做了两个独立功能包（**两台机互为备份/校验**，雷达为主、VL53 为备）：

### `src/standoff_estimator_pkg`（ament_cmake，主）
- `standoff_estimator_node`：订 `/scan`，取**正前方扇区**（`sector_center_deg ± sector_half_width_deg`）
  的激光点做 PCA 直线拟合（货架是实板→点落成一条线）。
  - 发 `/standoff/distance`(Float32,米)=原点到板面垂距；`/standoff/board_angle`(Float32,弧度，
    0=正对，可顺便判 yaw 摆正)；`/standoff/valid`(Bool)。
  - 纯 laser_link 系算，**不依赖 tf/cartographer**，雷达一上来就能用；带一遍离群剔除 + EMA 平滑。
  - 关键参数：`sector_center_deg`（雷达 x 轴与相机朝向有偏差时用它对齐）、`sector_half_width_deg`、
    `min_points`、`max_range_m`、`outlier_thresh_m`、`ema_alpha`。
  - 启：`ros2 launch standoff_estimator_pkg standoff_estimator.launch.py`
- **前提**：场地两面都是实板、不镂空（已确认），2D 雷达平面才打得到板。

### `src/vl53_ranging_pkg`（ament_cmake，备份/交叉校验）
- `vl53_ranging_node`：解析维特 VL53-100 **串口模式**（默认 115200/10Hz）ASCII 输出
  `State;N` + `d: NN mm`，复用 `serial_comm` 裸流读取 + 逐行正则解析。
  - 发 `/vl53/distance`(Float32,米)、`/vl53/state`(Int32, 0=有效)、`/vl53/valid`(Bool)、
    `/vl53/distance_mm`(Int32,调试)。
  - **接线 = `/dev/ttyS3`（串口三）**。气压计 `data_comm_analysis` 链路**已停用**（代码保留但不启动），
    所以 ttyS3 让给 VL53 即可——只要 launch 不带 `data_comm_analysis` 节点，不会抢口。
  - VL53 普通模式量程仅 1.2m、受强光影响——只当雷达盲区时的备份。
  - 启：`ros2 launch vl53_ranging_pkg vl53_ranging.launch.py serial_port:=/dev/ttyS3`
- 串口分配现状：`/dev/ttyS3`=（原气压计，已停用）现给 VL53；`/dev/ttyS6`=STM32 飞控(921600)。
- 朝向：相机/指示激光/雷达全部正前方，故 `sector_center_deg=0` 默认就对，VL53 也正对板面。

### 接线状态
- ✅ `standoff_estimator` 已加进 `inventory_mission.launch.py`，与 qr_vision/qr_fine_tune 一起起。
- ✅ `qr_fine_tune` 已订阅 standoff 动态增益 + 修正轴映射（见上）。
- ⏳ 实飞前标定：见下方「sync 后待测量清单」。

### sync 后待测量 / 标定清单（按顺序做，填进对应参数）

> 全部改 `inventory_mission.launch.py` 里对应节点的 `parameters`（standoff_estimator / qr_fine_tune）。
> 改完不必重编(纯参数)，重启 launch 即可。每项填上实测值，别忘。

| # | 要测什么 | 怎么测 | 写到哪 | 实测值 |
|---|---|---|---|---|
| 1 | 相机水平视场角 HFOV | 把码贴墙，量相机到墙距离 D 和画面横向恰好框住的实宽 W，`HFOV=2·atan(W/2/D)`；或查相机规格 | `qr_fine_tune` 参数 `hfov_deg`（占位 60） | ____° |
| 2 | 相机垂直视场角 VFOV | 同上量竖直方向实高 H，`VFOV=2·atan(H/2/D)` | `qr_fine_tune` 参数 `vfov_deg`（占位 37） | ____° |
| 3 | 横移符号 `invert_body_y` | 让码偏画面右侧，看微调量要飞机往哪平移才对中；不对就翻转 | `qr_fine_tune` 参数 `invert_body_y`（默认 False） | True / False |
| 4 | 高度符号 `invert_body_z` | 让码偏画面下侧，看要升还是降才对中；不对就翻转 | `qr_fine_tune` 参数 `invert_body_z`（默认 True） | True / False |
| 5 | 雷达正前方角 `sector_center_deg` | 正对板面悬停，`ros2 topic echo /scan` 看板面在哪个角度；同朝正前方应=0 | `standoff_estimator` 参数 `sector_center_deg`（默认 0） | ____° |
| 6 | 期望 standoff 距离 | 试到识别清晰 + 指示激光落进 19cm 码窗的相机离板距离，定为 body_x 目标 | 航点 `scan_standoff_cm`（§5-1）+ 任务节点 | ____ cm |

验证（编译后，无需飞）：
- `ros2 topic echo /standoff/distance` 出数且≈卷尺量的真实距离；`/standoff/board_angle` 正对时≈0。
- `ros2 topic echo /vl53/distance`（先确认没起 data_comm_analysis）和 standoff 量级对得上。
- 手持码在相机前移动，看 `/qr_vision/fine_offset_body_cm` 的 y/z 方向和大小是否合理（x 应恒 0）。

---

## 9. 旋转飞控测试 + 坐标系研究（2026-06-05，新增，✅首飞旋转成功）

### 9.1 rotate_test 测试航线（复用 inventory_mission_node）
给 `inventory_mission_node` 加了第三种 `mode:=rotate_test`，**复用全套飞控对接**（发
`/target_position`、`active_controller=2`、`route_choice=1`、tf 判到点、yaw 容差推进、终点发
`/mission_complete` 并停控制器），不另写节点。航线（全为非盘点过渡航点，不开识别/激光）：
```
起飞 home@test_height(yaw0) → 前进 +x test_forward(yaw0)
→ 原地旋转 0→test_yaw（默认连续转；step<目标角则分段，每步到位停一下）
→ 保持 test_yaw 平移飞回 home → home 垂直降落（保持 test_yaw）
```
- 参数（改了不重编，重启 launch 即生效）：`test_height_cm`(100)、`test_forward_cm`(200)、
  `test_yaw_deg`(180)、`test_yaw_step_deg`(**180=连续转**，2026-06-05 首飞后由 90 改默认连续)。
- 文件：`inventory_mission_node.{hpp,cpp}` 加 `MissionMode::ROTATE_TEST` + `buildRotateTestWaypoints()`；
  新建 `launch/rotate_test.launch.py`（只起任务节点，**飞控链路 carto+uart+位置PID 照常另起**）。
- **最小飞控底座**：`my_launch/rotate_test_basic.launch.py`（carto + uart + 标准位置PID，
  不用面阵激光、z 走 /height）。偏航角速度上限 `max_angular_velocity` 做成可传参，默认 **30°/s**
  （180° 约 6s；试过 20°/s 太慢要 9s 已弃）。
- 启（两个终端，都先 source setup_dds.sh）：
  ```
  ros2 launch my_launch rotate_test_basic.launch.py            # 飞控底座（转速30°/s）
  ros2 launch inventory_control_pkg rotate_test.launch.py      # 航点（默认连续转180）
  ```
  - 想分段观察：`test_yaw_step_deg:=45`（这是分段，不是变慢）。
  - 想更柔和：`max_angular_velocity:=xx` 传给 `rotate_test_basic`（步长不控转速，转速归它）。
- ✅ 2026-06-05 首飞：旋转成功、速度可接受。原 step=90 是分段转（每到 90/180 停一下，因每个
  中间航点要满足 yaw 容差才推进）；±180 跳变已根治故撤掉分步保险，默认改连续转 180、转速保持 30°/s。
- ⚠ 「转到 180 后带偏航平移返航」会**真正触发带偏航平移**（= 盘点扫背面同款动作），验证 §9.3 的
  0x31 机体系旋转——仍需重点盯「转到180后往home飞方向对不对」。

### 9.2 ±180° 偏航跳变已根治（PID 角度模式）
**根因**：`PIDController::calculate()` 是通用 PID，比例项用的误差已在 `pid_controller.cpp:290`
`normalizeAngleDeg(target-current)` 归一化（走最短弧，OK）；但**微分项**用两拍误差直接相减
`(current_error_-prev_error_)/dt`，当归一化误差跨 ±180 边界跳变（物理只动 2°、数值从 +179 跳到
−179 差 358°）时，微分炸成 ~358/dt → kd 给出巨大偏航冲击 = 当年 cartographer "转疯"现象。
目标 180° 时误差在 current 过 0° 处跳；目标 0° 时在 ±180 附近跳，两个角都会踩。
**修复**：给 `PIDController` 加 `setAngular(bool)`；angular 时**误差与微分增量都按 (-180,180] 归一化**
（`error_delta = normalizeAngleDeg(current_error_-prev_error_)`）。`pid_yaw_.setAngular(true)`，
xy/z 通道不变（不会跨 ±180）。对正常无跳变情况行为完全不变，只在跨界那一拍修正。
文件：`pid_control_pkg/pid_controller.{hpp,cpp}`。

### 9.3 ✅ 已定论并修复：/target_velocity（帧 0x31）飞控要的是**机体系**，已补旋转
**结论（2026-06-05 经验+代码铁证定性，已直接修，未试飞）**：飞控速度环要机体系速度。铁证是同仓
0x32 `/velocity_map`（**当前速度**反馈）已 `transformVelocity` 旋成机体系发给飞控；闭环要求目标与
反馈同系 → 0x31 目标速度也必须机体系。原 0x31 没旋（发 map 系），只在 yaw≈0（map==body）巧合可用，
盘点扫背面 yaw180 第一次会反向飞车。**已修**：`targetVelocityCallback`（`uart_to_stm32.cpp:294`）
照 0x32 把水平 (vx,vy) 用 `current_yaw_` 旋到机体系再发，vz/vyaw 不动；`yaw_valid_` 无效时回退原样
转发并告警（起飞瞬间 yaw≈0 安全）。仍需 rotate_test 实飞复核方向。下方原分析保留备查：

#### （原）待研究记录：/target_velocity（帧 0x31）到底是 map 系还是机体系？
**事实（代码已确认）**：`uart_to_stm32` 有两条速度路，处理坐标系**不一致**：
| 话题 | STM32 帧 | 是否旋到机体系 | 谁在用 |
|---|---|---|---|
| `/velocity_map` | 0x32 (VELOCITY_FRAME_ID) | **会**：`transformVelocity(vel, current_yaw)` 用 Rz 旋 | 老链路 |
| `/target_velocity` | 0x31 (TARGET_VELOCITY_FRAME_ID) | **不旋**：`targetVelocityCallback` 直接发串口 | PID→飞控，盘点/植保都走这条 |

PID（`pid_controller.cpp`）在 **map 系**算 `vel_x/vel_y`（误差=target−current，map 系），yaw 单独算。
所以 0x31 收到的是 **map 系速度**。能不能用,取决于 STM32 固件:
- 若固件对 0x31 **自己按地图系解释**（内部用 IMU 偏航旋到机体）→ 转任意角都对，盘点扫背面没问题。
- 若固件**期望机体系** → yaw=180 时 map 系 x 修正被反向执行 → **正反馈飞车**。

**为什么一直没暴露**：植保默认 `barcode_cam_axis="+x"`(yaw=0)，yaw≈0 时 map==body，两种解释结果一样。
spray 代码**支持** yaw=0/90/180/−90（`spray_mission_node.cpp:235-238`，看条码相机装哪轴），
但默认 +x、且注释提到"朋友老代码 yaw 实为 0"——**疑似从没真飞过 yaw≠0**，所以 0x31 在偏航下的行为
**至今未经验证**。盘点扫背面 yaw 180 是第一次真正触发。

**怎么定论（两条路）**：
1. 实测：跑 §9.1 rotate_test，盯「转到 180 后往 home 飞」——方向对=固件按 map 系(放心)；
   方向反/飞车=固件按机体系，**需在 `uart_to_stm32` 给 0x31 路径补 `transformVelocity(vel, current_yaw)`**
   （照 0x32 那条 `velocityCallback` 抄，注意 0x31 是 Float32MultiArray、0x32 是 Twist）。
2. 查 STM32 固件源码确认 0x31 处理（固件不在本仓，找队友）。
- 若需补旋转：改 `targetVelocityCallback`，在 `sendTargetVelocityToSerial` 前把 (vx,vy) 用
  `current_yaw_`(uart 已有该成员，`yaw_valid_` 判有效) 经 `transformVelocity` 旋一下；vz/vyaw 不动。
  ⚠ 但若固件本就按 map 解释，补了反而错——**务必先实测/查固件，别盲补**。

---

## 10. 下降途中提前打激光 bug 修复（2026-06-05，源自 24fly 老问题）

**现象（24fly）**：高行→低行是同一 y 的纯下降（如 A3(0,173,129)→A4(0,173,90)）。下降途中横向(y)
一直对正、`ex≈0`，二维码一能解码 `ex` 就进窗 → 还没降到该货位高度中心就提前打激光（光斑落到码外）+
提前判完成飞向下一个（123 低）。

**根因（两点）**：
1. `decoder_common.py` 激光触发 `laser_ready = abs(ex) < eps_x_laser` **只卡横向、不卡纵向**——
   纯下降时横向恒对正，故一解码就打。
2. 任务节点 `runScanAtWaypoint` 原以松的 `aligned`(eps 0.40) 判完成并推进，比激光窗口(0.25)还松 →
   可能"还没打激光就先判完成飞走"。

**修复（两步，只在升/降那次扫描加严，同行同高度横移行为不变）**：
- **识别端**：加 `eps_y_laser`(默认0.25) + 开关 `strict_vertical`（订 `/qr_vision/strict_vertical`）。
  仅 `strict_vertical=True` 时激光额外要求 `|ey|<eps_y_laser`（横纵都进窗才打）；False 时只卡横向，
  与旧版逐字节一致（同一行同高度横移、纵向本就到位，再卡 ey 怕相机/激光不共轴反而打不出）。
  激光打满 0.5s 灭后发 `/qr_vision/laser_fired`(String=刚打的码)。
- **任务节点**：①扫描开始时按"本货位高度是否≠上一航点高度(`vertical_entry_tol_cm`=5)"判断是否换行进入，
  换行才 `publishStrictVertical(true)`；②完成判据改为**等 `/qr_vision/laser_fired`**（不再用 aligned）→
  顺序恒为 对正→打满0.5s激光→记录→推进，不会提前飞走。超时(`scan_timeout_sec`)仍兜底跳过。
- 文件：`qr_vision_pkg/decoder_common.py`、`inventory_control_pkg/inventory_mission_node.{hpp,cpp}`。
- ⏳ 待另一台机编译 + 实飞复核（尤其 `eps_y_laser` 大小是否能在 19cm 码窗内稳定打出激光）。

---

## 11. 盘点超时→后退重试补救（2026-06-06，源于 D1 离板太近识别不到）

**现象**：实飞编译后**最后一个码 D1 识别不到**——离板太近时二维码占满画面/模糊解不出，6s 超时被跳过。

**补救**：`runScanAtWaypoint` 返回值从 bool 改 `ScanOutcome{WAITING/RECORDED/TIMED_OUT}`。
超时不再直接跳过，而是**沿机头反方向后撤 `scan_retreat_cm`(默认 10cm) 再试一次**（离板远些好解码）：
- **后退方向按面统一公式**：相机/激光/机头都朝板面 → 板面在 +机头方向 → 后撤 `-(cos yaw, sin yaw)`。
  A/C 面 yaw0 → x 减小（30→20、230→220）；B/D 面 yaw180 → x 增大（120→130、320→330）。y/z 不变（只加 standoff）。
  `retreatWaypoint(wp)` 实现，一个公式覆盖四面。
- **逐货位、只退一次**：`scan_retreated_`/`retreat_in_progress_` 两个标志，`advance()` 推进到下一货位即复位 →
  **只有超时那个码后退，其它码照常正常 standoff**。退了再超时就放弃跳过，不卡整套。
- **后退时收严激光**（`tighten_laser_on_retreat` 默认 true）：standoff 变大 → 同样归一化对准像素对应的
  **真实偏移变大**、激光窗口物理上变松 → 后退重试强制开 `strict_vertical`（激光须 ex+ey 都进窄窗才打）。
  万一相机/激光不共轴致 ey 门太严打不出，置 false 关掉。
- 视觉微调（`qr_fine_tune`）一直在跑，后退后靠它继续把 y/z 对正（增益随 standoff 动态变，见 §8）。
- `pos_tol_cm` 默认 **6cm**（2026-06-06 由 12 改回——12 是从旧笨重设备搬代码时放宽的遗留，本机不需要）。
  6 < 后退 10cm → 飞机要退到距后退点 6cm 内才算到达，会真把 10cm 退干净再扫，补救干脆。
- 参数都在 `inventory_mission.launch.py`（`scan_retreat_cm`/`tighten_laser_on_retreat`），纯参数不重编。
- 地面站状态栏后退时显「盘点中 货位X（后退重试）」。
- 文件：`inventory_mission_node.{hpp,cpp}`、`inventory_mission.launch.py`。⏳ 待编译 + 实飞复核 D1。

## 12. 定向返航绕板 + 定向同码识别不到（2026-06-06 实飞）

实飞要求2 暴露两个 bug，已修：

**① 定向返航斜穿货架板（差点撞板）**：原 `buildDirectedWaypoints` 是直线 `home→货位→黑圆(350,250)`，
返航那条直线从板前货位斜拉到黑圆会横穿货架。遍历靠"回y→平移→旋转"绕板，定向却没绕。
**修**：利用"飞机所在这一列 x 没有板子（板在 x75/x275，飞机在 x30/120/230/320，离板~45cm）"，
进出都**沿飞机本列 y 方向滑动**越过板尾 + 在板远侧空旷走廊横移：
- 去程：home 起飞到盘点高 → 沿 y=home_y 横移到本列 → 原地转向 → 沿本列爬进货位。
- 回程：沿本列爬到 `directed_return_y_cm`(默认 **300**，板 y≤180 留余量) → 横移到降落 x → 到黑圆上空 → 降落。
- **z 全程保持 scan.z**，只在黑圆正上方才垂直降落（不反复升降，用户要求）。比"退近侧 y=-20 绕一圈"回程省近一半。
- 新增参数 `directed_return_y_cm`(默认300，改不重编)。文件 `inventory_mission_node.{hpp,cpp}`。
- **地面站规划航线图也跟着绕板（2026-06-06）**：要求2-2 那张「规划航线图」是地面站用
  `slot_waypoints.json` 坐标**自己画**的（`main.py` `_orthogonal_route_points`），跟飞机实飞航点无关。
  原画法回程在**货位高度**直接横拉到降落点 → 戳穿 x=1.5/3.5 的板。已改成大致复刻飞机绕板：
  贴起飞底边滑到本列 → 沿本列爬进货位 → 继续爬到板外侧走廊 → 走廊里横移到降落 x。
  **横移段强制避开板的 y 跨度**（加固，防换坐标复发）：出发底边压到 `out_y = min(起飞y, 所有板 y_min-0.2)`、
  返航走廊抬到 `return_y = max(降落y, 所有板 y_max+0.2)`；竖段沿货位本列 x 爬升（设计上本列无板）天然不碰板。
  这样无论坐标标成什么值，横线都不会穿板。A/B/C/D 四面 24 货位经 `route_debug` 穿板检查**全部通过**。
  **只是示意**：坐标仍是占位值、跟飞机系没对齐(§6⚠仍在)，形状对、绝对位置会偏；且 2D 折线不画
  yaw，看不出 D 面 180° 掉头。文件 `diansai_terminal/ground_station/main.py`，改完要 sync 到地面站那块 pi。
  **脱机调试工具（2026-06-06）**：`ground_station/route_debug.py`（`python3 -m ground_station.route_debug`，
  地面站 pi 上先 source ROS humble；调路径用不到 DDS/飞机）——飞机不开机也能逐个调出 24 货位的规划
  航线图反复看，复用主程序同一个 `WarehouseMap`；右侧 A/B/C/D 各一行的货位按钮 + ←/→ 翻页，下方列折线
  坐标(米)+自动「穿板检查」(✅/⚠)。绘图逻辑抽到 `WarehouseMap.route_points_m(slot)`（返回米坐标折线），
  主界面 `_draw_route` 和调试工具共用，**改一处两边一致**——以后调画法只改这一个函数。

**② 定向到货位识别不到——同一个二维码数字（确诊）**：`decoder_common.py` 激光触发有去重
`qr_data != previous_qr_data`，而 `previous_qr_data` 跨货位不复位。定向**先在地面识别抽取码**(编号N)，
飞到的目标货位那张码**也是N**（定向本质=同号）→ 到货位后被去重挡住、激光永不触发 → 一直识别不到到超时。
遍历不踩因 24 码各不同。**修**：`/qr_vision/enable` 关→开上升沿（=每次新货位扫描开始）复位
`previous_qr_data/last_qr_id/stable_count`，每次扫描从干净态起，前后同码也能重新打激光。文件 `decoder_common.py`。

⏳ 三处都待编译 + 实飞复核。

## 进度

- [x] 切 `inventory-task` 分支（从 **spray-task**，底座含 G题 spray 代码）
- [x] 移植 `qr_vision_pkg`（单相机识别+激光+微调）
- [x] 新建 `inventory_control_pkg` 任务状态机骨架 + launch
- [x] 另一台机 colcon build 验证编译（2026-06-03 通过；含 standoff_estimator_pkg / vl53_ranging_pkg）
- [x] 补全缺失的 `vl53_ranging_pkg/src/vl53_ranging_node.cpp`（2026-06-03，原仓库只有 CMakeLists/launch/package.xml，
      源文件从未落地致 `add_executable` 找不到源 → 编译中断；现照 §8 用 serial_comm start_async_read 裸流+正则逐行解析补全）
- [x] `inventory_control_pkg` 加 `traverse_faces` 参数（2026-06-03）：默认 "A,B,C,D" 全跑；只有货架1 时
      `traverse_faces:=A,B` 只扫前后两面，不会飞向未到的货架2（buildTraverseWaypoints 按此列表逐面建点）
- [x] 板面距离测量双方案落地（§8：雷达 standoff 为主 + VL53 串口解析为备）
- [x] qr_fine_tune 修正轴映射(ex→body_y,ey→body_z,body_x归standoff) + 接 standoff 动态增益
- [ ] standoff 实测试通（另一台机编译后）+ 标定 HFOV/VFOV / invert 符号
- [x] 相机方位 + 激光引脚标定（2026-06-04，§5-2/5-3）：相机=下视 video0 `rotate_code=2`+`CAP_V4L2`；
      激光=WiringOP `gpio` 命令 `laser_pin=13`(on低)，实机 QR→激光链路跑通（`qr_laser_test.launch.py`）
- [x] 激光链路改 WiringOP gpio 命令行（弃 python wiringpi 模块 + 弃植保 /electromagnet_control 0x33）
- [x] autostart_fly.sh 从 G题 spray 切到 D题盘点（默认起 qr_laser_test.launch.py，录盘点话题）
- [x] 场地坐标标定（§5-1）：✅四面全实测写进 `faceGeometry()`（A:x30/y70,123,173/z129,90;
      B:x120转180/y75,128,178/z133,91; C:x230/y71,123,174/z132,90; D:x320转180/同y/z132,91；
      A/B 飞机x 由 0/150 各往板挪 30→30/120；蛇形扫描+换面三步；落黑圆(350,250)）；
      ✅货位号↔码对应已确认（四面统一无镜像：上排123高z/下排456低z；y小→1,4 / y大→3,6）
- [x] `inventory_full.launch.py`（§7，2026-06-05）：一把起飞控底座(carto+uart+位置PID)+视觉+任务，
      默认 traverse_faces=A,B,C,D + land_at_circle=true 落黑圆(350,250)；任务节点加 land_at_circle 开关
      （false 保留 A,B 单货架测试落末面y轴）。⏳ 待另一台机编译 + 整套试飞
- [ ] 换面过渡航点拆分 + 降自转速（§三建议1/3，回实验室结合实测坐标做；cartographer 朋友实测直接转也OK）
- [x] rotate_test 旋转飞控测试航线（§9.1，2026-06-05）：inventory_mission_node 加 mode:=rotate_test
      + rotate_test.launch.py（起飞→前进→分步转 test_yaw→保持偏航返航→降落，参数化不重编）
- [x] ±180° 偏航跳变根治（§9.2，2026-06-05）：PIDController 加 setAngular，yaw 误差+微分都按最短弧归一化
- [x] ✅ /target_velocity(帧0x31) 坐标系定论+修复（§9.3，2026-06-05）：飞控要机体系；铁证=0x32当前速度
      反馈已旋机体系、闭环要同系。已在 targetVelocityCallback 给 0x31 补 transformVelocity 把 (vx,vy)
      旋到机体系（vz/vyaw不动，yaw无效回退原样转发）。⏳ 待 rotate_test 实飞复核返航方向
- [~] rotate_test 实飞（2026-06-05 首飞旋转成功、速度可接受）：默认改连续转 180、转速保持 30°/s
      （试 20 太慢弃）；新增最小飞控底座 `my_launch/rotate_test_basic.launch.py`（max_angular_velocity 可传参）。
      ⏳ 待重编后再测一次连续转 + 复核返航方向
- [x] 地面站 LCD/LED + 编号查询（§6）——队友 PyQt5 实现，已并入 `diansai_terminal/`
- [x] 地面站触摸屏适配（§6，2026-06-03）：内置悬浮数字键盘 `NumericKeypad`+`TouchLineEdit`（无系统输入法也能输 1~24）
      + UI 放大（左侧三按钮/搜索框/LED/标签页）；厘清「查询坐标=要求1-4 手动」vs「定向盘点=要求2 自动」
- [x] 定向盘点 编号→货位 方案定型（§5-4）：地面站权威查表 + 下发货位 `/inventory_target_slot`
- [x] buildDirectedWaypoints 写实 + slotToScanWaypoint 遍历/定向共用几何
- [x] 和队友对接话题接口（§6）：改地面站去贴飞机话题，统一 6 个话题 + 格式，单测 7/7
      （含 2026-06-05 新增 `/inventory_mode` 地面站→飞机任务模式）
- [x] 跨机 DDS 配置（§7）：两端 `scripts/setup_dds.sh`，FastDDS + DOMAIN_ID 26
- [x] 两机 DDS 实测连通（2026-06-03，topic 互见 OK）
- [x] 地面站自启脚本 `diansai_terminal/scripts/autostart_ground_station.sh`（2026-06-03，对称 autostart_fly.sh：
      等系统起→source ROS humble + setup_dds.sh→全屏起 ground_station.main；加进香橙派「会话与启动」；
      飞机端 autostart_fly.sh 第84行早已 source setup_dds.sh，不需另加）
- [ ] 地面站 ↔ 飞机两机联调（DDS 已通；剩真机话题闭环：飞机发 /inventory_result→地面站建表、定向下发货位回路）
- [ ] 地面站 `slot_waypoints.json` 坐标与飞机 slotToScanWaypoint 对齐到同一地图系（§6 警告）
- [x] 下降途中提前打激光 bug 修复（§10，2026-06-05）：decoder 加 eps_y_laser+strict_vertical 纵向门控
      （只换行那次开）+ 发 /qr_vision/laser_fired；任务节点按高度差判换行开严判、完成判据改等激光打满。
      ⏳ 待编译 + 实飞复核 eps_y_laser 大小
- [x] WAIT_MODE + 地面站下发任务模式（§4/§6，2026-06-05）：飞机重启后原地待命不起飞，地面站「普通/进阶」
      大按钮发 `/inventory_mode`(traverse/directed) 才起飞；任务节点加 `MissionPhase::WAIT_MODE` + `modeCommandCallback`
      + `configureForMode()`；地面站 ros_bridge 加 `publish_mode` + main 左侧大按钮 + fake_uav 同步等模式。
      顺带根治 autostart 上电即起飞。⏳ 待编译 + 两机联调验证按钮链路
- [x] 地面站重启一律清空（§5-4/§6，2026-06-05）：`MainWindow.__init__` 调 `clear_results()`；
      `InventoryStore` 类落盘/load 保留给单测（7/7 仍过），app 启动不再 load 旧 json
- [x] D 面 y 整体 +4（2026-06-05 实飞全打中后微调）：faceGeometry D 列 y=75/127/178
- [x] 飞机状态/心跳上地面站（§6，2026-06-06）：飞机发 `/inventory_status`(~0.5s)，地面站顶栏「飞机在线/离线
      + 当前阶段」+ 识别抽取码 `✅已识别` 反馈；fake_uav 同步发。比赛只能看地面站 → 解决"重启起没起好/
      识别没识别到"两个盲点。⏳ 待编译 + 两机联调。
- [x] 盘点超时→后退重试补救（§11，2026-06-06）：超时不直接跳过，沿机头反方向后撤 scan_retreat_cm(10)
      离板远些重试解码（按 yaw 统一公式四面通用），逐货位只退一次、退完正常；后退时收严激光(strict_vertical)。
      源于实飞 D1 离板太近识别不到。⏳ 待编译 + 实飞复核。
- [x] D1 单货位 y -3（§5-1，2026-06-06）：slotYAdjustCm() 单货位覆盖，不动同列 D4。
- [x] 定向地面识别期间控制器提前接管隐患修复（2026-06-06）：原离开 WAIT_MODE 头一拍就发 active_controller=2，
      但定向先进 IDENTIFY 在地面读码/等货位(数秒~十几秒)却没发 target_position → 位置 PID 空跑默认目标可能地面乱动。
      改 `first_publish_done_` 触发条件加 `phase_ != IDENTIFY`：识别完进 TAKEOFF 那拍(有航点了)才接管。遍历不受影响。
- [x] qr_vision 待命态也出预览窗（§7，2026-06-06）：原 detect 关闭时主循环提前 return 不出画面 →
      改 `_render_output` 待命态也显示（叠 STANDBY/SCANNING 字样）；`enable_gui` 经 launch 透传，
      `inventory_full` 默认 true（平时远程/VNC 看飞机屏确认自启动+相机；无头省CPU传 false）。
- [x] 地面站物理 LED 灯（§6，2026-06-07）：和飞机激光同套 WiringOP `gpio` 接口（pin13/on低）驱动物理灯，
      屏幕方块保留；新增 `ground_station/gpio_led.py`（GpioLed，子线程亮灭1s不阻塞）+ `gpio_led_test.py` 自测脚本
      + main 集成 + CLI `--led-pin/--led-on-level/--led-off-level`。开发板要装 WiringOP + 加 gpio 组。
      ✅ 联调验证亮灭通过（踩坑：DDS 脚本必须 source 不能 `./`；fake_uav 先起 main 再起它最后点按钮防打空炮）。
- [ ] 整套试飞
