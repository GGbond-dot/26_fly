# 5-23-02-54 飞行日志总结

## 基本信息

- 日志目录: `/home/kian/kian_26fly/log_fly/5-23-02-54`
- 记录文件:
  - `metadata(1).yaml`
  - `bag_20260523_024948_0.db3`
  - `run_20260523_024948.log`
- ROS 版本: Humble
- 记录开始时间: 2026-05-23 02:49:49 左右
- bag 时长: 157.99 s
- bag 消息总数: 52,738
- 运行日志结束位置: 约 2026-05-23 02:52:27，任务未见完整收尾/降落完成记录

## Bag Topic 概览

| Topic | 类型 | 消息数 | 说明 |
| --- | --- | ---: | --- |
| `/height` | `std_msgs/msg/Int16` | 31,628 | 飞控/高度相关数据，消息量最高 |
| `/laser_array/ground_height` | `std_msgs/msg/Int16` | 8,893 | 面阵激光地面高度 |
| `/target_velocity` | `std_msgs/msg/Float32MultiArray` | 7,365 | 目标速度输出 |
| `/fine_data` | `std_msgs/msg/Int32MultiArray` | 2,349 | 视觉/精细数据 |
| `/circle_area_ratio` | `std_msgs/msg/Float32` | 2,349 | 铁片占比 |
| `/target_position` | `std_msgs/msg/Float32MultiArray` | 153 | 目标位置 |
| `/detected_pillars` | `std_msgs/msg/Float32MultiArray` | 1 | 柱子检测结果 |
| `/mission_step` | `std_msgs/msg/UInt8` | 0 | 未记录任务步骤消息 |

## 节点启动情况

日志显示以下主要节点已启动:

- `uart_to_stm32_node`: 串口 `/dev/ttyS6`，921600 baud，负责速度、舵机、电磁铁、声光控制转发。
- `position_pid_controller`: 50 Hz 位置 PID 控制器，速度限制 linear=33 cm/s、angular=30 deg/s、vertical=30 cm/s。
- `pillar_pickup_mission`: PICKUP 两趟任务，模式为 `full`。
- `pillar_detector_tf`: 基于 TF 的柱子检测。
- `visual_node`: camera=0，640x480，process_fps=15。
- `laser_array_ground_node`: 面阵激光高度节点。
- `bluesea2_node` 与 Cartographer 相关节点: 激光雷达和建图链路启动。

## 任务流程摘要

1. 起飞阶段
   - 任务发布 `takeoff` 目标: `(0.0, 0.0, 30.0)`。
   - 起飞阶段早期持续出现 `map -> laser_link` TF 查找失败告警，随后 robot_state_publisher 启动并发布 `laser_link` 等 segment。
   - 约 02:50:35，`takeoff` 航点完成。

2. 扫描与柱子检测
   - 发布 `scan_end`: `(250.0, 0.0, 30.0)`。
   - 柱子检测累计 130 帧、472 个候选点。
   - 检测出 4 根柱子:
     - 柱 1: `(0.633 m, -0.806 m)`，票数 130
     - 柱 2: `(1.666 m, -0.790 m)`，票数 130
     - 柱 3: `(1.725 m, -1.664 m)`，票数 121
     - 柱 4: `(0.820 m, -1.940 m)`，票数 91
   - 任务侧收到 `/detected_pillars: 4 个柱子`。

3. 第一趟测高与铁片占比
   - 系统规划 4 根柱子，并判定空柱/放置目标为 `#2 @ (172.5, -166.4)`。
   - 测量结果:
     - 柱 1: 铁片占比 0.050，有片，柱高 61.0 cm
     - 柱 2: 铁片占比 0.000，空柱，柱高 73.0 cm
     - 柱 3: 铁片占比 0.392，有片，柱高 49.0 cm
     - 柱 0: 铁片占比 0.173，有片，柱高 86.0 cm
   - 第一趟完成后待抓取 3 片，按占比从大到小排序:
     - `#1 柱3 @ (82.0, -194.0)`，占比 0.392
     - `#2 柱0 @ (63.3, -80.6)`，占比 0.173
     - `#3 柱1 @ (166.6, -79.0)`，占比 0.050

4. 第二趟抓取与叠放
   - 第 1 片:
     - 目标柱: `(82.0, -194.0)`。
     - 抓取前现场测高 49.0 cm。
     - 分段下降后进入 `HOVER_GRAB`，机械臂伸出、电磁铁吸附。
     - 约 02:51:37 记录 `抓取成功（尝试 1/3）`。
     - 随后前往空柱 `(172.5, -166.4)`，进入 `HOVER_DROP`，机械臂伸出并松磁，完成一次放置动作。
   - 第 2 片:
     - 目标柱: `(63.3, -80.6)`。
     - 抓取前现场测高 38.0 cm。
     - 分段下降目标包括 `z=120.0`、`z=90.0`、`descend_final z=61.0`。
     - 日志在 `descend_final` 阶段继续重发目标，最后高度读数回到约 11 cm，但未出现 `HOVER_GRAB`、`抓取成功` 或后续放置记录。
   - 第 3 片未见开始记录。

## 异常与风险点

- WARN 数量: 11
  - 主要集中在启动早期的 TF 查找失败:
    - `TF map <- laser_link 失败`
    - `Failed to lookup transform map -> laser_link`
  - 这些告警出现在 robot_state_publisher/TF 链路完全就绪前，后续任务仍能继续扫描和检测。
- ERROR 数量: 1
  - `rviz2` 进程退出，exit code `-11`。这影响可视化，不一定直接影响飞控任务。
- 高度数据存在异常值
  - `uart_to_stm32` 多次读到 `Current height from flight controller: 32767`。
  - 该值疑似无效/哨兵值，建议在后续分析中确认是否被控制逻辑过滤。
- 任务未完整结束
  - 日志没有记录任务完成、第三片抓取、最终降落或 `/mission_complete`。
  - bag 记录持续约 158 秒后结束，结束时仍处于第 2 片 `descend_final` 相关状态。

## 结论

本次日志记录了一次 PICKUP 两趟任务的主要过程。系统完成了起飞、扫描、4 柱检测、第一趟测高与占比判定，并成功抓取和放置了第 1 片铁片。第 2 片进入下降抓取末段后日志结束，未看到抓取成功、放置、第三片处理或降落完成记录，因此本次任务从日志看属于未完整闭环。

优先建议排查:

1. 为什么日志/bag 在第 2 片 `descend_final` 阶段结束。
2. 飞控高度 `32767` 是否为无效值，以及控制链路是否正确过滤。
3. 启动早期 TF 失败是否会影响任务初期定位稳定性。
4. `rviz2` 段错误是否为偶发可视化问题，还是资源/配置问题。
