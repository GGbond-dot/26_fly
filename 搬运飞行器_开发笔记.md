# 搬运飞行器 开发笔记（交接文档）

> 用途：把这份文档导入新对话即可继续开发。记录了题目、硬件/传感器设计、
> 代码架构、本轮已实现的两趟逻辑、待标定常量、以及未完成的 TODO。
> 最后更新：2026-05-23 深夜追加（**PID 接管修复 + 空柱视觉 anchor + 下一轮对比实验待办**，见第18.6/18.7节：
>   ① 修复视觉接管链路：mission 同时发布 `/visual_takeover` 与 `/visual_takeover_active`，PID 已能进入 visual takeover；
>      PID 视觉轴映射按文档修正，并把视觉 PID 调温和（deadzone 10px、max_vel 6cm/s）；
>   ② 最大铁片**抓取**跳过视觉微调，避免抓得太正导致空气动力抖动；最大铁片**放置**仍走空柱视觉对齐；
>   ③ 空柱放置新增 `drop anchor`：空柱必须靠视觉确认，视觉对齐成功后记录当前实际 map 位姿，后续下降/爬升/叠放优先复用该 anchor；
>      anchor 用 `empty_pillar_side_cm=32` 做最大修正约束，并用近期圆盘识别 veto，防止放完后视觉锁到铁片；
>   ④ 下一轮实验：分段视觉微调不一定有效，要把“分段中停微调”和“中心对准后直接下降抓取”解耦成可切换方案做 A/B；
>   ⑤ 下一轮放置建议：不要完全贴到空柱再松磁，先对准中心，直接降到空柱上方约 10cm，伸机械臂，松磁，悬停 1s，减少惯性影响。）
> 上一轮更新：2026-05-24（**放置时序修复 + 放置分段对准 + 抓取精细化**，见第18节：
>   ① 放置 `HOVER_DROP` 时序 bug：原本伸臂与松磁**同帧发**，机械臂伸直需 1.2s、还没伸直就松磁→铁片提前掉；
>      改成先伸臂→等 `arm_extend_sec`(1.2) 臂到位→再松磁→再等 `drop_settle_sec`(0.5) 落稳→收臂；
>   ② 放置落点多压 2cm：`drop_press_cm`(2)，z=R+空柱高+已叠+drop_gap−drop_press（注：是放到空柱上的下降，不是降地面）；
>   ③ 放置也走多段中停对准（复用抓取机制 + `descend_is_drop_` 标志）：视觉已确认空柱顶**只有黑框无圆**对准照常
>      （demo3 center_source=square 走框几何中心），第一片放置无问题；叠片时柱顶黑盘可能干扰框"洞"判定→列实飞风险；
>   ④ 抓**最小片**分段加密 `descend_seg_len_min_plate_cm`(20)，仅抓取末位最小片，放置不加密（平台大）；
>   ⑤ 抓取末段盲降加 y 负向偏置 `grab_final_dy_cm`(−2)，让电磁铁吸得更准）
> 上一轮更新：2026-05-23 深夜（**第三次试飞(5-23-02-54)复盘：测高分簇+一致性校验+下降安全底线**，见第17节：
>   ① 视觉中心框优先(16.2)生效→占比/空柱判定恢复正常、第1片完整抓放成功；
>   ② 新问题=第2片(柱0,真高~90)抓取前现场测高崩成 38cm→下降目标 z=61 远低于应有~117→飞机狂降、用户手动中断；
>   ③ 修：`tryStepHeight` 中位数→**最高密集簇中位数**(柱顶给最大drop,不被柱沿低值拉偏)；
>      `MEASURE_HEIGHT_GRAB` 加**一致性校验**(现场值与第一趟差>`live_height_consistency_cm`=15→回退第一趟值)；
>      下降三段加**安全底线**(面阵<`descend_abort_area_cm`=22→中止下降爬回重试,不用人拔)；`height_min_hit_frames` 5→10）
> 上一轮更新：2026-05-23 晚（**第二次试飞复盘改 2 处**，见第16节：
>   ① `grab_press_cm` 0→**4**（分段下降版实飞臂尖停柱顶面上方约4cm够不到，需多压4cm）；
>   ② 视觉**中心框优先**：对齐柱①时起停区 A 的大方框(50cm)挤进画面边角、被纯面积打分抢锁→占比/对准跑偏(疑似第14节占比错乱根因)。
>      detector_core 方框 score 乘"离画面中心越近越高"的因子(`rect_center_bias`默认2.0)，压掉边角的 A/B 框；不碰圆检测/不解码A）
> 上一轮更新：2026-05-23（**抓取下降分段二次对准 + 去 z 偏置 + 电磁铁早开**，见第15节：
>   ① 抓取下降改**变段数中停对准**：每段降 30cm 对准一次，剩余<50cm 末段直接盲降(末段≈20~49cm)；放置不加；
>   ② `grab_press_cm` 2.0→**0**、`drop_gap_cm` 2.0→**0**（两个 z 下降偏置都去掉，加了会下降过头）；
>   ③ 电磁铁**进入下降第一段即通电**(全程磁吸)，`HOVER_GRAB` 改先磁后臂；放置侧仍先伸臂后松磁；
>   ④ 视觉"识别不到"复盘：是**黑方框本身丢**(非框在圆丢)，用户硬件给黑边加白边解决，代码视觉不动；
>   ⑤ `mid_clearance_cm` 抓取侧废弃，新增 `descend_seg_len_cm`/`descend_min_tail_cm`/`descend_recenter_timeout_sec`）
> 上一轮更新：2026-05-22 晚（**复盘 5-22-22-54 首次完整抓取试飞**，见第14节：
>   ① R 实测标定=27（第6/7节）；② /height 加 32767 无效值过滤（第3节）；③ measure_only_mode=False 跑完整流程；
>   ④ 修 bug：单片抓取3次失败原本直接降落→改为**跳下一片，全部片各试3次都失败才降落**（第5节）；
>   ⑤ grab_press 2.5→0（实飞稳定多降3-4cm，第6/7节）；
>   ⑥ **核心待办：视觉占比 /circle_area_ratio 错乱**——最大片(柱0)被测成占比最小、空柱(柱2)测出占比，
>      连累抓取顺序+对准+空柱判定，是问题根因，下次重测优先查 visual_node，见第14节）
> 更早更新：2026-05-22（**测高挪回第一趟**：点阵拉近相机(2.5cm)、第二趟伸臂会挡点阵→第二趟禁用 /height；
>   第一趟逐柱测好柱高(含空柱)存起来，第二趟下降只用面阵，见第3/5节；
>   **下降参照改 R**：新增标定量 ARM_GROUND_AREA_CM=机械臂伸直触地时面阵读数，抓取 z=R+柱高−grab_press，
>   放置 z=R+空柱高+已叠+drop_gap，废弃 grab_clearance，见第6节；cam_offset -5.1→-2.5；
>   arm_offset 机械结构解决：机械臂伸直后吸取点与相机光轴共线→真值0/0，见第6/9节；
>   新增声光提示：第一趟找到铁片→开蜂鸣器/LED(帧0x22)+滞空3s，见第3/8节；
>   测高判定从"连续3帧一致"改为"零散命中累计取中位数"，见第13节；
>   复盘 5-22-13-51 试飞：救回损坏 rosbag，定位 pillar3 漏测真因=点阵激光没稳定打中窄柱顶）
> 上次更新：2026-05-21（核对飞控侧机械臂/电磁铁+高度协议；cam_offset 重标为 -5.1/0；
>   新增试飞日志脚本 scripts/run_test.sh；当前 measure_only_mode=True 测高试飞档）

---

## 1. 题目（2026 杭电电赛 X 题 搬运飞行器）

- 场地：A、B 为 50×50cm 对角起停区；场内 4 个边长约 15cm、高 50~150cm、顶面白色的高台。
  其中 **3 个高台上各放一片黑色圆铁片（直径约 5 / 8 / 11cm）**，1 个高台为空。
- 高台中心距边界 ≥50cm，高台间距 ≥80cm。
- **基础要求**：随机指定 A 或 B 起飞 → 一键起飞 → 降落到对角起停区；记录并显示起飞到降落的时间。
- **发挥要求**：起飞 → 自主找黑铁片（找到滞空 3s + 声光提示）→ 搬到那个空高台，
  **大的在下、小的在上**叠放 → 完成后降落到对角起停区。
- 启动后不得人为干预。

---

## 2. 场地坐标与约定（已写死进代码）

- 起飞点 = map 原点 (0,0)，扫描终点 (scan_end_x, 0) = (250, 0) cm。
- 降落点（对角 B）= (landing_x, landing_y) = (250, -250) cm。
- **空高台 = 离 B 最近的那根**（场地我们自己布置，已固定这个约定）。代码用
  `nearestToLanding()` 选它作放置目标。
- 检测有效 bbox（map 系，米）：x∈[0.25,2.25], y∈[-2.25,-0.25]（见 pillar_detector launch/param）。
- **实际横截面**：空柱（放置目标）= **30×30cm**，顶面宽容、叠放/落点余量大；
  其余载片柱 = **比 15cm 略大**，是取物对准较紧的一环（arm_offset 已机械共轴=0，靠视觉对中心要稳）。
- **所有柱顶都有黑色方边框**（空柱也有，只是没铁片）→ 视觉对所有柱子都能锁边框对中心；
  占比 = 圆/边框框面积，空柱采不到圆 = 占比缺失 = 空柱信号。

---

## 3. 硬件 / 传感器设计（关键，别改错）

### 两个激光的分工（重要结论）
- **面阵激光**（`laser_array_pkg`，节点 `laser_array_ground_node`，串口 /dev/ttyS3）：
  16 束，取 `max(valid)` 当地面 + 双峰检测 + 全遮挡冻结（`max_hold_frames` 帧）。
  **设计上抗突变、只认地面、刻意无视柱子** → 用作**飞控的 z 高度反馈**。
  话题 `/laser_array/ground_height`（Int16, cm）。
- **点阵激光**（单点，经 STM32 回传，uart_to_stm32 把它发到 `/height`，Int16, cm）：
  打到柱顶会有清晰阶跃 → 用作**测柱子高度**。
  ⚠️ **2026-05-22：点阵拉近相机到 2.5cm，第二趟伸臂会挡点阵 → `/height` 只能第一趟用**
  （机械臂收起、点阵无遮挡）。第二趟下降禁用 `/height`，只用面阵。
- **测柱高公式**（`tryStepHeight`，**第一趟 SURVEY 的 MEASURE_HEIGHT 测，逐柱含空柱，存进 survey_results_[i].height_cm**）：
  `drop = (面阵地面读数 - LASER_MOUNT_DIFF_CM) - 点阵读数`；
  **窗口内累计所有 drop≥`sample_pillar_drop_thresh_cm`(20) 的"命中帧"，攒够 `height_min_hit_frames`(默认5) 帧
  即取中位数为柱高，不要求连续**（2026-05-22 改，见下方"测高判定改版"）。
  早先是"连续3帧突变且帧间差≤5"，但窄柱顶激光反复滑进滑出、命中帧零散，连续条件难满足→漏测。
- **为什么用占比判铁片大小而不是直径**：柱高不统一→直径像素随高度变；
  而"圆面积/方框面积"占比与高度无关，所以用 `/circle_area_ratio` 排大小。

### 关于"面阵停在柱顶会不会突变"的结论
- 面阵冻结只在 **16 束全打在柱子上（无任何束见地面）** 时触发，且只撑 `max_hold_frames`(默认20≈0.4s)。
- 只要还有任意一束见地面 → `max()` 咬住真地面，**不冻结不漂移**。
- 点阵与面阵物理间距 ~20cm，对柱子正上方时面阵阵列大概率仍扫到旁边地面 →
  现实几何下"全遮挡"基本不发生。**先预设面阵不被遮挡**，出问题再处理。
- z 反馈全程留在面阵（飞控内部要吃稳定高度，不能突变，这是底线）。

### 执行机构
- 舵机机械臂：`/servo_control`（UInt8，0x01 伸出/放下 / 0x00 收起，串口帧 0x11）。
- 电磁铁：`/electromagnet_control`（UInt8，0x01 吸 / 0x00 松，串口帧 0x33）。已从 GPIO 迁到 STM32。
- 声光提示（蜂鸣器+LED）：`/buzzer_led_control`（UInt8，0x01 开 / 0x00 关，串口帧 **0x22**，蜂鸣器与 LED 绑同一帧）。
  硬件：PD15→蜂鸣器，PD14→WS2812B LED DIN。上电 uart 与 mission 各发一次 0x00 关闭。

### 飞控固件侧协议（2026-05-21 已与下位机核对，上位机无需改动）
匿名帧格式：`0xAA | addr | ID | dataLen | data... | sc1 | sc2`；
`sc1=前(dataLen+4)字节累加和`，`sc2=sc1 逐次累加和`（serial_comm 的 build/checksum 与之一字不差）。
- **0x11 舵机**：`data[0]==1 → TIM4_CH1(PD12)=1260 舵机下/抓取位`；其它 `→500 收起`。
- **0x33 电磁铁**：`data[0]==1 → PD13 置位(吸)`；其它 `→ 复位(松)`。
- **0x07 面阵地面高度（上位机→飞控）**：2B int16 LE cm → `rosData.laser_height_cm`，
  飞控再封 **0x34 距离帧(direction=0,angle_100=270,distance=该高度)** 经 UART5 喂 IMU 作 z 反馈。
- **0x05 高度（飞控→上位机→`/height`）**：固件外发的是 `ano_of.of_alt_cm`（激光测高模块/光流口的对地单点高度），
  **实测打到柱顶有清晰阶跃**，第一趟 `tryStepHeight` 测柱高靠它（早先笔记说的"点阵单点"即此路）。
  注意：这条已不是 `fc_alt.st_data.add`。⚠️ 第二趟伸臂会挡它 → 第二趟不订阅/不用这条测高。

---

## 4. 节点 / 话题数据流

```
pillar_detector_tf  --(/detected_pillars, 仅xy)-->  mission
visual_node         --(/fine_data 像素偏差, /circle_area_ratio 占比)--> mission
laser_array_ground  --(/laser_array/ground_height 面阵)--> mission & uart(→飞控帧0x07)
uart_to_stm32       --(/height 点阵, 来自STM32帧0x05)--> mission
mission(pillar_pickup_mission)
   --(/target_position [x,y,z,yaw])--> PID --(/target_velocity)--> uart --> STM32(飞控)
   --(/visual_takeover, /active_controller, /servo_control, /electromagnet_control)
   --(/pillar_detect_enable)--> detector
   --(/route_choice=1)--> uart (打开 /target_velocity 转发，否则不下发)
   --(/mission_complete)--> uart (帧发3次, 关闭转发)
```

- **检测本身不直接发飞控指令**；检测只给 xy。mission 收到后切目标点，经 PID→uart→飞控。
- uart 必须先收到 `/route_choice ∈{1,2,3}` 才转发速度；mission 启动时连发 10 次 `/route_choice=1`。

---

## 5. 本轮已实现：两趟任务逻辑

文件：`src/activity_control_pkg/{include/activity_control_pkg/pillar_pickup_mission_node.hpp, src/pillar_pickup_mission_node.cpp}`
启动：`src/my_launch/launch/demo3.launch.py` → 含 `pillar_pickup_mission.launch.py`

**关键设计（本轮重点）：摄像头对准的是"画面中心"，不是激光/机械臂。**
摄像头、点阵激光、机械臂吸取点在机体上是三个不同位置，所以分两个偏置、各站各的位：
- `cam_offset`（相机→点阵激光）：**测高那步**用，把激光挪到柱心。
- `arm_offset`（相机→机械臂吸取点）：**下降取物/放置**用，把吸取点挪到铁片心。
吸取后机械臂提起，不遮挡摄像头，放置时空柱照样能视觉"对中心"（空柱高已在第一趟测好，放置不再测高）。

状态机：
```
SCAN (起飞(0,0,flight_h) → 扫描终点(250,0)，到起飞点开检测、到终点关检测)
 → WAIT_PILLARS (等 /detected_pillars 聚类结果, 超时则直接降落)
 → SURVEY  第一趟【测占比 + 测柱高】，逐柱（就近遍历，含空柱）：
      APPROACH(飞到柱上方 pillar_visit_height)
      CENTER(接管对准边框/铁片中心 + 采集 /circle_area_ratio 占比;
             找到铁片→开声光滞空 survey_signal_hold_sec(3s)→关声光)
      MEASURE_HEIGHT(关接管, +cam_offset(-2.5)把点阵挪到柱心, 累计命中帧测柱高存储;
                     超时→该柱高度未知, 第二趟跳过/空柱不能放)
      记录每柱 {plate_ratio, has_plate, height_cm, has_height}
 → planPickupOrder(): 排除空柱和无片柱，按 plate_ratio 降序 = 大→小
 → PICKUP  第二趟，按大→小逐片【用第一趟柱高, 只用面阵, 不碰 /height】：
      APPROACH → CENTER(接管对准铁片中心 xy; 无 has_height 则跳过此片)
       → MEASURE_HEIGHT_GRAB(抓取前现场测高，超时回退第一趟柱高) → startGrabDescend()
       → 【分段下降，2026-05-23】DESCEND_MID(降到下一中停读数, 进入即开电磁铁)
           ⇄ RECENTER_MID(中停二次对准方框中心, 对上‖超时往下) … 循环到无中停点
       → DESCEND_FINAL(末段盲降 z=R+柱高−grab_press)
       → HOVER_GRAB(先磁后臂) → CLIMB_BACK
       → OBSERVE_GRAB(靠占比判成败=爬升后还看到圆[非NaN]即铁片还在=失败;
           同片失败重试≤pickup_max_attempts 回 MEASURE_HEIGHT_GRAB(重测高+重建分段); 单片用尽→**跳下一片**(pickup_iter++、attempts清零);
           全部片各自试满都失败才 startLanding; 2026-05-22 修，原来单片失败即降落)
       → GOTO_DROP(空柱; 空柱高未知则带片降落)
       → CENTER_DROP(接管只对空柱 xy, 高度用第一趟测好的)
       → DESCEND_DROP(z=R+空柱高+stack_count×厚度+drop_gap)
       → HOVER_DROP(伸臂+松) → CLIMB_AFTER_DROP, stack_count++
 → LAND (对角起停区 land_approach→land_hover→land)
 → DONE (/mission_complete)
```
（R = ARM_GROUND_AREA_CM = 机械臂伸直触地时面阵读数；z 全是面阵目标读数，见第6节）

要点：
- 大下小上 = 占比大的先抓先放垫底。
- 叠放每多一层落点抬高 `plate_thickness_cm`。
- 第一趟 `MEASURE_HEIGHT` 先 settle 到激光位(柱心)再采样，避免移动途中扫掠污染。
- 两个干跑开关现已等效（测高在第一趟）：任一为 true → 只跑第一趟(测高+占比)然后降落、不抓取。
  正式跑抓取叠放前两者都设 False。

---

## 6. 待标定常量

### 编译期宏（在 hpp 顶部，改后必须重新编译）
| 宏 | 含义 | 占位值 | 标定法 |
|---|---|---|---|
| `LASER_AREA_BASE_CM` | 面阵贴地读数（脚架触地）；用于算 MOUNT_DIFF | 13.0 | 飞机贴地读 `/laser_array/ground_height` |
| `LASER_POINT_BASE_CM` | 点阵贴地读数 | 4.0 | 飞机贴地读 `/height` |
| `LASER_MOUNT_DIFF_CM` | 面阵-点阵 z 向高差（**第一趟**测高修正量） | 9.0(=两者相减) | 也可直接量两激光发射点 z 差 |
| **`ARM_GROUND_AREA_CM`(R)** | **机械臂伸直、臂尖触地时的面阵读数**；第二趟下降的唯一参照 | **27.0(已实测 5-22)** | 悬停到臂尖刚触地，读 `/laser_array/ground_height` |

- ⚠️ 重装过激光，`A_g/P_g/MOUNT_DIFF` 三个都要重测；`R` 是新标定量，必须实测。
- 下降全在面阵读数域：抓取 `z=R+柱高−grab_press`，中停 `z=R+柱高+mid_clearance`，
  放置 `z=R+空柱高+已叠层×plate_thickness+drop_gap`。R 一个数顶替旧 `LASER_AREA_BASE+grab_clearance` 拼凑。

### launch 参数（改 `pillar_pickup_mission.launch.py`，不用重编）
- `cam_offset_dx_cm / dy_cm`：摄像头→**点阵激光**的 xy 偏置（机体系=map系，yaw全程0）。
  **轴映射**：map +x ↔ 画面正上方，map +y ↔ 画面正左方。`offset = 相机−激光`。
  当前 **-2.5 / 0**（2026-05-22：点阵激光拉近相机到 2.5cm，仍在“画面正上方”=map +x 侧）。
  （历史：5-21 为 -5.1；更早旧装在“画面正左方”=+y侧时为 0 / -5。均与轴映射自洽。）
  **只在第一趟 MEASURE_HEIGHT 用**（第二趟不测高），把点阵挪到柱心。
  ⚠️ 符号未实飞验证：若试飞发现激光打偏方向相反，把 dx 取正(=+2.5)。
  （点阵↔面阵的 xy 间距**不需要**单独修正，平地上柱高公式只吃 z 向 mount_diff。）
- `arm_offset_dx_cm / dy_cm`：摄像头→**机械臂吸取点**的 xy 偏置（机体系）。**真值 0 / 0（已由机械结构解决，非占位）**。
  **只在下降取物/放置用**（DESCEND_MID/FINAL/DROP），把吸取点挪到铁片心。
  2026-05-22：舵机机械臂伸直后吸取点与摄像头光轴共线 → 吸取点 xy = 相机中心 xy →
  铁片对到画面中心即可竖直下降取物，无需平移。若实飞发现伸臂后仍有微小残差，再在此填小量微调。
- 测高参数（**第一趟 MEASURE_HEIGHT**用）：`measure_height_timeout_sec`(5) / `height_min_hit_frames`(5)；
  命中阈值复用 `sample_pillar_drop_thresh_cm`(20)。
  （旧 `height_step_frames`/`height_step_consistency_cm` 已废弃删除，2026-05-22。）
- 下降参数（launch，z 全是面阵目标读数）：
  - **抓取下降已改为分段中停对准（2026-05-23，第15节）**：`descend_seg_len_cm`(30)/`descend_min_tail_cm`(20)/`descend_recenter_timeout_sec`(2.5)。
    `mid_clearance_cm`(30) **抓取侧已废弃**（保留声明避免参数报错）。
  - `grab_press_cm`(**0**, 5-23：实飞加偏置会下降过头→去掉=臂尖≈柱顶面)；调节方向：还嫌压→设负值(如-1.5停柱顶上方1.5cm)；反而够不到吸不牢→回调正值(1~2)。
  - `drop_gap_cm`(**0**, 5-23：放置偏置同样去掉)。
  （旧 `grab_clearance_cm` 已废弃，被 R 参照取代，2026-05-22。）

---

## 7. 关键参数一览（launch 现值）

- 容差：position 9cm / yaw 5° / height 12cm
- 高度：flight(扫描)30 / land 20 / pillar_visit 150 / landing(250,-250)
- 测高（第一趟 MEASURE_HEIGHT）：最低命中帧 5（取中位数，不要求连续）/ 命中阈值 20cm / 超时 5s
- 偏置：cam_offset -2.5/0（点阵拉近相机，画面正上方=map+x侧，符号待实飞验证）/ arm_offset 0/0（**机械共轴，真值即0**）
- 占比采样：plate_min_ratio_frames 3（少于则判空柱）
- 声光提示：survey_signal_hold_sec 3.0（第一趟找到铁片后开声光+滞空 3s，帧 0x22）
- 下降抓取：R=ARM_GROUND_AREA_CM(**已实测 27**, 5-22) / **分段下降 seg30+tail20+recenter超时2.5s(5-23)** / grab_press **0**(5-23) / drop_gap **0**(5-23) / hover_grab 1.0s / plate_thickness 1.0（mid_clearance 抓取侧废弃）
- 抓取观察重试：observe 2.0s / max_attempts 3
- traverse_only_mode / measure_only_mode：现等效，任一 True = 只跑第一趟(测高+占比)然后降落、不抓取；**当前两者都 False=跑完整抓取叠放**(5-22-22-54起)。
  抓取叠放正式跑前两者都设 False。

---

## 8. 未完成 / 下一步 TODO

1. **飞控下降抓取协议**（用户在研究）：识别到柱子后怎么给飞控发数据让"下降 + 舵机机械臂动作"。
   接入口已隔离：
   - `enterPickupSub(HOVER_GRAB)` 内 `publishServo(true); publishMagnet(true);`
   - `enterPickupSub(HOVER_DROP)` 内 `publishServo(true); publishMagnet(false);`
   - 若下降也交给飞控，则 `DESCEND_FINAL` 那段一起替换。
2. ~~**声光提示 + 找到滞空 3s**~~：**已做（2026-05-22）**。第一趟 SURVEY 的 CENTER 里，
   占比帧够（=确认有铁片）即开声光(帧0x22)、起算滞空，悬停 `survey_signal_hold_sec`(3.0s) 后关声光再飞下一柱；
   空柱（占比不够）走原超时退出、不响。声光时长=滞空时长（声光持续整 3s）。3s 从"找到"那刻起算、不从进 CENTER 起算。
   抓取/放置/降落的可选提示**暂未加**（用户只要题目硬性那条）。
3. **基础要求计时显示**：靠 `/mission_complete` 帧给 STM32，由下位机计时显示（需确认 STM32 侧实现）。

## 9. 待验证 / 调参风险（代码没擅自改）

1. ~~**arm_offset 未标定（当前最大风险）**~~：**已解决（2026-05-22，机械结构）**。机械臂伸直后吸取点
   与摄像头光轴共线 → arm_offset 真值 0/0，铁片对到画面中心即可竖直下降取物，不再是风险。
   残余风险：伸臂后若仍有微小残差需实飞观察，必要时给 arm_offset 填小量微调（载片柱余量小，对准仍要稳）。
2. **测高已挪回第一趟（2026-05-22）**：点阵拉近相机、第二趟伸臂会挡点阵 → 第二趟禁用 `/height`。
   第一趟 `SURVEY/MEASURE_HEIGHT` 逐柱测好柱高(含空柱)存 `survey_results_[i].height_cm`，第二趟复用。
   traverse_only / measure_only 现等效（都只跑第一趟测高+占比然后降落）。
   ✅ **R(ARM_GROUND_AREA_CM) 已实测 = 27**（2026-05-22 臂尖触地稳定段面阵中位27/均27.23/σ0.49，220445.csv）。
   残余：实飞抓取若发现差几 cm，结合 grab_press 微调即可。
3. **撞柱**：柱高现在 ~110cm，`pillar_visit_height=150` 有 ~40cm 余量，基本可消；仍以实际柱高为准。
4. **grab_press / drop_gap 待实飞调**：抓取下压 2.5cm 是否吸牢、放置留 2cm gap 是否掉歪，看试飞再调。
5. **空柱判定依赖"离 B 最近"**：靠场地布置保证；若某非空柱占比没采到会被当空柱漏抓。
6. **测高超时策略**：取物侧超时→放弃该片不下降（跳下一片）；放置侧超时→带片飞去降落区不盲降。
   柱顶宽（空柱30、载片柱>15）+ 激光不被遮挡，超时实际很难触发。

---

## 10. 开发环境备注

- **双设备开发**：本机只改代码、**不在本机 colcon build**，编译在另一台。
- 编译/运行机是 **ROS2 humble**，环境已在 `.bashrc` source 好，跑命令/脚本里别再 source。
- 工作目录 `/home/kian/kian_26fly/src`，git 分支 main。
- `/home/kian/kian_26fly/Intel_Nezha_ws` 是外部参考拷贝（同名包的另一份），**不改、不当本项目**。
- 题目 PDF：`/home/kian/kian_26fly/2026年杭州电子科技大学电子设计竞赛题-搬运飞行器.pdf`

## 11. 试飞日志脚本 `scripts/run_test.sh`

一键起 launch + 全量留日志，Ctrl-C 一起收尾。同步源码 + `colcon build` 后：
```bash
cd ~/kian_26fly
./scripts/run_test.sh                       # 默认 demo3.launch.py
./scripts/run_test.sh demo_basic.launch.py  # 换 launch（余下参数透传 ros2 launch）
FLY_LOG_DIR=/mnt/u盘 ./scripts/run_test.sh   # 改日志目录
```
- 产物默认在 `~/fly_logs/`：`run_<时间戳>.log`（控制台 tee，带时间戳、行缓冲）+ `bag_<时间戳>/`（rosbag）。
- 录的话题（数值排查用）：`/height` `/laser_array/ground_height` `/circle_area_ratio`
  `/fine_data` `/detected_pillars` `/target_position` `/target_velocity` `/mission_step`。
- 事后：`ros2 bag info/play ~/fly_logs/bag_<时间戳>`；要加减话题改脚本里 `TOPICS=(...)`。

## 12. 摄像头/激光坐标轴标定（cam_offset 推导依据，2026-05-21）

- **图像像素轴**（visual_node，已实测）：`/fine_data=[dx,dy]`，目标右移→dx↑、下移→dy↑（OpenCV 右+下+）。
- **建图(map)系 ↔ 图像**（用户给定，且与旧实测 cam_offset 自洽）：
  `map +x ↔ 画面正上方`，`map +y ↔ 画面正左方`（即图像竖直轴=map x，图像水平轴=map y）。
- `cam_offset` 在 **map 系**填（applyCameraOffsetToTarget 把 dx/dy 当 map x/y 分量），
  靠"选哪个 map 分量赋值"吸收图像↔map 的 90° 旋转，不另写转换矩阵。
- 推导：激光在画面正上方=map +x 侧 → `offset=相机−激光=(-d,0)`，d=相机↔点阵；
  2026-05-22 拉近到 d=2.5cm → `(-2.5, 0)`（5-21 时 d=5.1cm → -5.1）。
- 还可不起飞验方向（测试2）：手推飞机 map+x→画面下、map+y→画面右；吻合则符号也对。

## 13. 测高判定改版（2026-05-22，基于 5-22-13-51 试飞复盘）

### 背景：那次试飞 pillar3 被"采样不足"跳过
- 现象：4 柱全检测到，但 pillar3 在对准窗口里只采到 6 帧、被判"采样柱子帧不足"跳过；
  飞机没卡住（约 4s 对准超时后自动跳下一柱），所以现场肉眼没看出异常。
- 复盘数据来源：试飞 rosbag `bag_20260522_133115_0.db3` 当时报 `database disk image is malformed`，
  原因是**文件被截断**（头部声明 413 页，实际只有 385 页，尾部 ~28 页≈114KB 缺失，
  多半是 recorder 没 flush 完就拷走/Ctrl-C）。把头部页数改写成实际页数后，
  **按 rowid 顺序流式读出了 31189 条消息（覆盖整段任务，最后一条 ts≈任务完成前）**，
  恢复物在 `log_fly/5-22-13-51/recovered/`（可播放 bag + 各话题 CSV）。
  → 教训：飞完等 recorder 正常退出再拷；保留 `metadata.yaml`。
- 真因（用恢复数据算 `drop=(面阵-9)-点阵`）：

  | 柱 | 视觉帧(到达) | 点阵打中柱顶占比(drop≥20) | 命中帧高度中位数 |
  |---|---:|---:|---:|
  | pillar1 | 16 | 36.7% | 27cm |
  | pillar2 | 61 | 59.2% | 53cm |
  | **pillar3** | **61** | **7.6%** | **35cm** |
  | pillar4 | 24 | 31.2% | 77cm |

  - 视觉话题（`/circle_area_ratio`、`/fine_data`）pillar3 满帧 61 帧、~15Hz、无空档 → **不是相机/视觉问题**。
  - 真因：**点阵激光 92% 时间在读地面，只有 7.6% 帧打中柱顶**（窄载片柱顶面+对准/cam_offset 精度）；
    打中的那 62 帧本身很稳（范围 27~37、标准差 3.8）→ 数据质量够，问题是"命中零散、连不成连续帧"。

### 改动：`tryStepHeight`（mission_node.cpp）
- 旧：滑动窗口攒 `height_step_frames`(3) 帧，要求**全部 drop≥阈值 且 帧间差≤`height_step_consistency_cm`(5)**，取均值。
  → 命中帧零散时凑不出"连续 3 帧"，漏测。
- 新：**窗口内累计所有 drop≥`sample_pillar_drop_thresh_cm`(20) 的命中帧**（蹭地面/边沿帧直接丢弃），
  攒够 `height_min_hit_frames`(默认5) 帧即 **取中位数** 为柱高，**不要求连续**；超时仍没攒够才放弃该片。
  - 仅在 settle 到激光位（`isReached`）时才累积，避免移动扫掠污染（沿用旧设计）。
  - 用恢复数据回算：pillar3 命中 62 帧≫5、中位数 35cm → 新逻辑能正常测出，不再跳过。
- 参数变更：删 `height_step_frames`/`height_step_consistency_cm`，加 `height_min_hit_frames`(launch 默认 5)。
- ⚠️ **治标提醒**：这只让"零散命中也能测出高度"，真因（激光没稳定落柱顶）没解决。
  cam_offset（现 -2.5）要标准，让点阵稳定落柱顶，命中帧越多测高越稳。
- 📌 **2026-05-22 之后语境变化**：此 `tryStepHeight` 现在跑在**第一趟** `SURVEY/MEASURE_HEIGHT`
  （机械臂收起、点阵无遮挡），柱高存起来第二趟复用；第二趟下降已**不用点阵/不调 tryStepHeight**，
  改用面阵参照 R（见第 6 节）。所以"下降对准偏"现在取决于**视觉对中心 + arm 共轴**，不再是点阵打偏。
  本节命中率分析仍适用于第一趟测高质量。

## 14. 首次完整抓取试飞复盘（5-22-22-54）

日志位置：`log_fly/5-22-22-54/`（run log + bag + codex 分析 `5-22-22-54_飞行日志数据分析.md`）。
本次首次 `measure_only_mode=False` 跑完整流程：起飞→扫描→检出4柱→第一趟测占比+柱高(4/4成功)→
第二趟抓第1片→3次失败→（旧逻辑）直接降落。

### 现场实物 vs 视觉/代码（核对用户口述）
| 柱 | 坐标(x,y)cm | 视觉占比 | 测得柱高 | 实物(用户口述) | 代码判定 |
|---|---|---:|---:|---|---|
| 0 | (61.6,-82.7) 离起点最近 | **0.023(最小)** | 85 | **最大片(11cm)** | 第3片去抓 |
| 1 | (164.6,-84.8) | 0.088 | 59 | — | 第2片去抓 |
| 2 | (168.6,-172.0) 离B最近 | **0.237** | 37 | **空柱** | 当空柱(离B最近,选对) |
| 3 | (76.6,-213.2) | 0.292(最大) | 37 | — | 第1片去抓 |

### 三个问题与定位
1. **抓取顺序不对（先飞了"错"的柱子）**：mission 排序逻辑没问题（`planPickupOrder` 按占比降序、
   排除空柱），先抓占比最大的柱3。**真因是上游视觉 `/circle_area_ratio` 错乱**：
   实物最大片在柱0 却被测成占比最小(0.023)，空柱柱2 反而测出 0.237。→ 按错占比排序，先飞错柱。
2. **抓取下降太低**：用户实测稳定多降 3-4cm。已把 `grab_press` 2.5→0（臂尖≈柱顶面）。
   注：用户说手量柱高与激光有误差，不纠结柱高绝对值，只看下降净偏差。
3. **单片失败就回终点**：已修——单片3次失败→跳下一片，全部片各试满都失败才降落（第5节）。

### 抓取成功判定（已澄清，逻辑无bug）
- `visual_node.py` 无目标/被遮挡时发 `NaN`；mission OBSERVE_GRAB 期间收到非NaN(看到圆)=铁片还在=失败，
  收到NaN(遮挡)不判失败。**符合"吸到→遮挡→识别不到=成功"**。
- 这次3次全失败是**真没吸到**：observe 期间持续收到非NaN占比(中位0.084、最大0.378、100帧)，
  即爬升后相机仍看得到柱顶的圆=铁片没被带走。不是误判。
- 隐患（未改）：单帧非NaN即判失败，偏敏感；未来可加"累计看到圆帧数≥N才判失败"防单帧噪声。本次非单帧问题，优先级低。

### 🔴 核心待办（下次重测最高优先）：查 visual_node 占比错乱
- 占比能把最大片测成最小、给空柱测出占比 → 圆心 `/fine_data` 大概率也偏 →
  连累 ①抓取顺序 ②抓取对准(吸不到) ③空柱占比矛盾，是**三个现象的共同上游**。
- 建议：先单独验证视觉，录 `/circle_area_ratio`+`/fine_data` 逐柱原始值；
  静态查 `src/visual_pkg/visual_pkg/visual_node.py` 的 `RobustRatioFilter` / 圆框面积比计算。
- 视觉不修，整机重测大概率仍先飞错柱+吸不到。

### 其它日志暴露的风险（codex 分析第11节，待确认）
- 任务完成(168s)后 bag 到214s 仍有 `/target_velocity` 输出 → 确认 `/mission_complete` 是否真发、uart 是否停转发。
- 启动前12s map TF 未建立、RViz 崩溃（exit -11）→ 不阻断任务，正式飞可不开 RViz。

### 本轮代码改动汇总（待编译机 colcon build）
| 文件 | 改动 |
|---|---|
| `pillar_pickup_mission_node.hpp` | `ARM_GROUND_AREA_CM` 33→**27**（实测） |
| `pillar_pickup_mission_node.cpp` | `/height` 加 32767 无效值过滤；单片失败→跳下一片+换片重置 attempts |
| `pillar_pickup_mission.launch.py` | `grab_press_cm` 2.5→**0**；`measure_only_mode` True→**False** |

## 15. 抓取下降分段对准 + 去 z 偏置 + 电磁铁早开（2026-05-23，已改未编译）

承接第14节复盘，针对用户三个实飞观察改：①对准后直接降很看运气→下降中分段二次对准；
②加了 z 下降偏置会下降过头→去掉；③要电磁铁早开、且先发磁再发舵机。另对"识别不到"做了诊断。

### 15.1 视觉"识别不到"诊断（代码未改，硬件解决）
- 现象：偶尔铁片识别不到，且**方框也一起识别不到**（不是"框在、圆丢"，是框本身丢）。
- 链路：visual_node 圆的检测被"先找到方框"门控（visual_node.py:165，只有方框 confirmed/hold 才进框找圆）；
  且 demo3 用 `center_source=square`，**对准信号 `/fine_data` 走方框几何中心**，`/circle_area_ratio` 只用于排序+抓取成败。
  所以方框丢=对准信号丢=圆也不找。
- 结论：根因是黑方框轮廓在反光/模糊/斜视角下没被认成"4角凸多边形+有孔"。
  **用户给黑边外面再加一圈白边**（拉对比度、让黑框轮廓更易闭合成框）解决，代码视觉侧不动。
  备查的次因：visual_node.py:181 `circle_for_pub=circle_current` 没接 `circle_hold_frames`(32)→圆单帧丢即 NaN；
  以及黑铁片高光会在 HSV 黑掩码上打洞。若加白边后仍丢，再考虑接 hold(注意会影响 OBSERVE 的"看不到圆=成功"判定)。

### 15.2 抓取下降：变段数中停二次对准（核心）
- 流程：CENTER → MEASURE_HEIGHT_GRAB → `startGrabDescend()` →
  `DESCEND_MID`(降到下一中停读数) ⇄ `RECENTER_MID`(开视觉对方框中心，对上‖`descend_recenter_timeout_sec`=2.5s 超时往下) → 循环 → `DESCEND_FINAL`(末段盲降 z=R+柱高−grab_press) → HOVER_GRAB。
- 分段算法 `buildDescendPlan(ph)`（cpp）：总下降量 D = `pillar_visit_height`(150) − (R+柱高−grab_press)；
  贪心：**剩余 ≥ seg+tail(30+20=50) 就下降 seg(30) 设一个中停对准点**，否则末段直接盲降到位。
  → 末段落在 [tail, seg+tail) ≈ **[20,49]cm**；用户规则"49 不分、50 可以"由此而来。
  - 实测柱高对应段数（R=27,grab_press=0,巡航150）：柱高37→D86→2点；柱高59→D64→1点；柱高85→D38→0点(一把降)。
- **防卡死**：RECENTER_MID 必带超时；且方框若丢，visual_node 发 `[0,0]`→mission 视作已对准→直接往下，不会卡。
- **放置侧不加**对准（空柱30×30余量大）：CENTER_DROP→DESCEND_DROP 不变。
- 新参数（launch）：`descend_seg_len_cm`(30)/`descend_min_tail_cm`(20)/`descend_recenter_timeout_sec`(2.5)；
  `mid_clearance_cm` 抓取侧废弃（保留声明）。新成员：`descend_checkpoints_`/`descend_ckpt_idx_`；新方法 `buildDescendPlan`/`startGrabDescend`。

### 15.3 去掉两个 z 下降偏置
- `grab_press_cm` 2.0→**0**、`drop_gap_cm` 2.0→**0**（launch）。实飞加偏置都会下降过头。
  调节方向不变（grab_press 还嫌压设负、够不到回正；drop_gap 同理）。

### 15.4 电磁铁早开 + 先磁后臂
- **进入抓取下降第一段(`DESCEND_MID`)即 `publishMagnet(true)`**，下降全程保持磁吸；`DESCEND_FINAL` 再确认一次。
- `HOVER_GRAB` 顺序改成**先 `publishMagnet(true)` 后 `publishServo(true)`**（接触瞬间已有磁吸）。
- **放置 `HOVER_DROP` 维持先伸臂后松磁不变**（"先磁后臂"只为抓取早充磁，放置先松磁会提前掉片）。
- 抓取段磁吸生命周期：DESCEND_MID 开 → 抓到后全程保持(HOVER_GRAB收臂但磁吸留) → HOVER_DROP 才松；
  单片全部尝试失败才 `publishMagnet(false)` 收臂跳下一片。

### 15.5 修的隐患
- `DESCEND_FINAL` 原来只改 `sub_target_.z_cm` 复用上一段 xy；**无中停点(短下降)直接进 FINAL 时**会沿用
  MEASURE_HEIGHT_GRAB 设的带 `cam_offset` 的 xy（点阵柱心，不是机械臂吸取点）→ 现 FINAL 重设完整 xy 并 `applyArmOffset`。

### 本轮代码改动汇总（待编译机 colcon build）
| 文件 | 改动 |
|---|---|
| `pillar_pickup_mission_node.hpp` | 加 `PickupSub::RECENTER_MID`；加成员 `descend_checkpoints_`/`descend_ckpt_idx_`；加参数 `descend_seg_len_cm_`/`descend_min_tail_cm_`/`descend_recenter_timeout_sec_`；`mid_clearance_cm_` 标废弃；声明 `buildDescendPlan`/`startGrabDescend` |
| `pillar_pickup_mission_node.cpp` | 实现 `buildDescendPlan`/`startGrabDescend`；`DESCEND_MID` 改降中停点+早开磁；加 `RECENTER_MID`；`DESCEND_FINAL` 补 xy+arm_offset+确认磁；`HOVER_GRAB` 先磁后臂；测高两路转 `startGrabDescend()` |
| `pillar_pickup_mission.launch.py` | `grab_press_cm` 2.0→**0**；`drop_gap_cm` 2.0→**0**；新增 `descend_seg_len_cm`(30)/`descend_min_tail_cm`(20)/`descend_recenter_timeout_sec`(2.5) |

## 16. 第二次完整试飞复盘改 2 处（2026-05-23 晚，已改未编译）

承接第15节那版（分段下降+去偏置+电磁铁早开）实飞，用户复盘出两个问题，对症各改一处。

### 16.1 抓取下降偏置加回来：`grab_press_cm` 0→4
- 现象：第15节去掉了 grab_press（设0），但分段下降版实飞**臂尖停在柱顶面上方约4cm够不到、吸不牢**。
  （注：和第14节"2.5太低多降3-4cm"方向相反——那是旧的直接盲降版；分段下降版末段几何不同，落点偏高。）
- 改：`pillar_pickup_mission.launch.py` `grab_press_cm` 0→**4**。约定不变：`z=R+柱高−grab_press`，
  grab_press 越大降得越低。还够不到→继续加正值；反而压坏→回调（最小0或设负值停柱顶上方）。

### 16.2 视觉"中心框优先"：压掉起停区 A 方框抢锁（疑似第14节占比错乱真因）
- 现象：对齐**离起点最近的柱①**时，左下角**起停区 A**（粗黑线围的≈50cm 角区方框 + 里面空心粗线字母 A）
  从画面边角挤进来被**误识别**。
- 真因（判定为「锁错框」非「认错圆」）：A 角区那个粗黑线方框正是 `detect_black_rectangle_border` 的菜
  （四角凸四边形+粗黑边+中间有洞）；而方框 `score` 几乎只看**面积**（detector_core.py:490），
  A 框 50cm 远大于柱顶黑框（柱①>15cm）→ 一挤进画面 score 就压过柱框，`StableRectangleTracker` 锁到 A。
  demo3 是 `center_source=square`，对准信号 `/fine_data` 跟锁定框中心 → 飞机往 A 偏；
  "圆"又在 A 框里找 → 占比也错。**这八成就是第14节"占比错乱/最大片测成最小/空柱测出占比"的共同上游。**
- 为什么不"解码 A"：`/apriltag_code` 是假的（visual_node.py:108-111 把静态参数原样发，demo3 设 -1），
  没有真 tag 检测；地上是喷的字母，OCR 不划算。且铁片=实心圆盘、A=画面边角的框，不用认字就能区分。
- 改法：方框打分加**画面中心优先**因子——离画面中心越近分越高，把边角的 A/B 大框压下去。
  飞机在柱①正上方对齐时柱框在画面**正中**、A 框在**边角**，权重一压 A 出局；锁不到 A 则圆只在柱框里找，
  占比/对准/抓取顺序一起恢复。对所有柱子受益（永远优先正下方那个框）。配合 tracker `switch_required_hits=3` 迟滞更稳。
  - 公式：`score *= center_score**rect_center_bias`，`center_score=1−中心距/半对角线`（正中1→画面角0）。
    `rect_center_bias` 默认 **2.0**；0=关闭(纯面积旧行为)；试飞嫌还抢锁就加大(3~4)。
  - 风险低：CENTER 阶段飞机已 `isReached` 到柱① xy（容差9cm），柱框本就贴近画面中心；A≥50cm 开外离中心远。
  - **不碰圆检测逻辑**（如试飞发现万一被填实的 A 仍漏过，再叠"圆度/solidity 收紧"做第二道防线）。

### 本轮代码改动汇总（待编译机 colcon build）
| 文件 | 改动 |
|---|---|
| `pillar_pickup_mission.launch.py` | `grab_press_cm` 0→**4** |
| `visual_pkg/detector_core.py` | `DetectorConfig` 加 `rect_center_bias`(默认2.0)；`detect_black_rectangle_border` score 乘画面中心优先因子 |
| `visual_pkg/visual_node.py` | 声明参数 `rect_center_bias`(默认2.0) 并写入 `self._config` |
| `my_launch/demo3.launch.py` | visual_node 加 `rect_center_bias`(2.0) 方便现场调 |

## 17. 第三次完整试飞复盘（5-23-02-54）：测高分簇 + 一致性校验 + 下降安全底线（2026-05-23 深夜，**后续已实飞验证、高度问题解决**）

日志：`log_fly/5-23-02-54/`（run log + bag + 日志总结.md）。本次跑的是第16节改动版（grab_press=4 + 中心框优先）。

### 17.1 进步（已验证）
- **视觉占比错乱(第14节核心待办)基本修好**：空柱(柱2)占比测出 **0.000 干净**且被正确选为放置目标（上次空柱误测 0.237）；
  占比梯度合理 0.392/0.173/0.050，排序不再把最大片测成最小。→ 16.2「中心框优先」压掉起停区A抢锁**生效**。用户现场确认 A 不再误识别。
- **第1片完整「抓→放」首次跑通**：柱3(49cm)尝试1/3一次抓成并放到空柱。grab_press 0→4 + 分段下降 + 电磁铁早开这套对该片 work。用户据此确认偏置没问题。

### 17.2 新问题：第2片(柱0)抓取前现场测高崩了 → 狂降
- 现场实物：柱0 是最高那根，用户手量 ≈90cm（手量与激光有系统差，量级对）。
- 数据：柱0 **第一趟测高 86cm(对)**，**抓取前现场重测 38cm(错,差48cm)**。
- 因果：下降目标 `z=R(27)+38−grab_press(4)=61`，正好=日志 descend_final z=61.0。但柱0 要让臂尖到顶应是面阵≈`27+90=117`。
  飞机拿着远低的 61 当目标 → **一直往下降、降过头**（面阵一度掉到 ~11，比贴地还低）→ 用户怕出事手动中断、bag 在 158s 断。
- 为何现场重测会崩：`tryStepHeight` 旧版攒够5帧 drop≥20 即取**中位数**。窄高柱顶点阵反复滑进滑出，命中帧分「柱顶簇(高,~86)」+「柱沿/半遮挡簇(低,~30-40)」两簇，
  柱沿帧偏多就把中位数拉到 38——**自信地返回一个危险的低值**。第1片柱3矮(49)柱顶易打中，没中招。

### 17.3 三处修改（病根 + 双保险，用户拍板"保留重测"）
1. **测高估计：中位数 → 最高密集簇的中位数**（`tryStepHeight`）。物理上点阵打中柱顶正中给最大 drop，打偏只会更小→
   真柱顶=drop 最高的那一簇。排序按 `height_cluster_gap_cm`(8) 间隔切簇，在帧数≥`height_min_cluster_frames`(3) 的簇里取**中位数最高**的簇（≥3帧滤单帧近读毛刺），取其中位数。
   方向安全：偏高最多够不到可重试，绝不偏低撞柱。一次排序+一遍扫描，O(n log n)、n才几十，算力可忽略。第一趟测高同样受益。
   - 兜底：若无任何簇够帧 → 返回 false 继续攒，超时由调用方回退第一趟柱高。
   - `height_min_hit_frames` 5→**10**（保证至少能凑出一个够3帧的柱顶簇）。
2. **抓取前现场重测加一致性校验**（`MEASURE_HEIGHT_GRAB`）：现场值与第一趟柱高差 >`live_height_consistency_cm`(**15**) →
   判现场不可信，`has_pickup_live_height_=false` 回退用第一趟 survey 柱高下降（第一趟机械臂收起、点阵干净、停留久，最可信）。
   保留现场重测是用户要求（之前不重测常出问题，能抓两趟间柱子检测xy漂移），只是给它套个安全网。好情况两趟差~4cm，暴雷差48cm，15 拦得住又留激光互差余量。
3. **下降安全底线**（`stepPickup` switch 前的 guard）：DESCEND_MID/RECENTER_MID/DESCEND_FINAL 三段中，
   面阵读数 < `descend_abort_area_cm`(**22**≈R−5，臂尖将到地面以下=物理不可能) → 立即中止下降，当作一次抓取失败：
   能重试(`pickup_attempts_<max`)就回 `MEASURE_HEIGHT_GRAB`（其目标z=巡航150，自动爬回重测重试）；用尽则松磁收臂发 `/pickup_failed` 跳下一片（与 OBSERVE_GRAB 失败收尾一致）。
   **不用人去拔电**。短柱正常抓取(如50cm柱目标z=73)面阵不会低于22，不误触发。

### 17.4 防御纵深（三道叠加）
柱沿暴雷 → ①分簇优先选柱顶簇救回；万一柱顶簇没够3帧 → ②一致性校验对第一趟，差>15 回退好值；万一第一趟也没测到高 → ③下降中面阵<22 安全底线刹车。任一道兜住即可。

### 17.5 仍待办/次要（沿用，未恶化）
- `/height` 32767 已在 mission 端 `pointHeightCallback`(cpp line 202) 过滤；uart 端那条 print 是串口侧原始打印、不进控制，是否需下位机侧也处理待确认。
- `/mission_step` bag 里 0 条（可能根本没发布或没录到），低优先。
- 启动早期 TF map→laser_link 告警、rviz2 崩溃(exit -11)：不阻断任务，正式飞可不开 rviz。
- 更治本的选项(本轮没做)：把现场重测**挪到下降第一段之后**（离柱顶近、光斑小、命中率高、更准）。先等本轮三道防线试飞验证再决定要不要加。

### 本轮代码改动汇总（待编译机 colcon build）
| 文件 | 改动 |
|---|---|
| `pillar_pickup_mission_node.hpp` | 加参数成员 `height_cluster_gap_cm_`/`height_min_cluster_frames_`/`live_height_consistency_cm_`/`descend_abort_area_cm_`；更新 `tryStepHeight`/`height_hit_samples_` 注释 |
| `pillar_pickup_mission_node.cpp` | `tryStepHeight` 改最高密集簇中位数；`MEASURE_HEIGHT_GRAB` 加一致性校验回退第一趟值；`stepPickup` 加下降安全底线 abort+重试；declare 新参数，`height_min_hit_frames` 默认 5→10 |
| `pillar_pickup_mission.launch.py` | `height_min_hit_frames` 5→**10**；新增 `height_cluster_gap_cm`(8)/`height_min_cluster_frames`(3)/`live_height_consistency_cm`(15)/`descend_abort_area_cm`(22) |

## 18. 放置时序修复 + 放置分段对准 + 抓取精细化（2026-05-24，待新对话改代码）

> 本节是**待实现的需求清单**（不是复盘），承接第17节那版（分簇测高+三道防线，**已飞过验证、高度问题解决**）。
> 用户对放置侧和抓取精细化提出 5 项改动。下面逐项给出**根因、改法、要碰的函数、新参数**，照着改即可。
> 第17节三道防线已实飞确认有效，本节可直接在其基础上叠加实现。

### 背景：当前放置流程（第15节定的，放置侧"不加对准"）
`OBSERVE_GRAB`(抓成功) → `GOTO_DROP`(飞空柱上方) → `CENTER_DROP`(视觉对空柱框中心，仅 xy) →
`DESCEND_DROP`(**单段盲降** z=R+空柱高+已叠+drop_gap) → `HOVER_DROP`(伸臂+松磁) → `CLIMB_AFTER_DROP`。
本节把放置下降也升级成"分段中停对准"，并修 `HOVER_DROP` 的时序 bug。

### 18.1 放置释放时序修复（核心 bug）
- **现象**：`HOVER_DROP` 进入时 `publishServo(true)` 和 `publishMagnet(false)` **同一时刻发**（cpp `enterPickupSub` 的 `HOVER_DROP` case，line ~991-999）。
  机械臂从收起到伸直需要 **~1.2s**，现在臂还没伸直磁就松了 → 铁片提前脱落、放不准。
- **改法**：把放置释放拆成时间序列（机械臂动作与松磁解耦）：
  1. 进入 `HOVER_DROP`：只 `publishServo(true)`（开始伸臂），记 `sub_hover_start_`，**不再同帧松磁**。
  2. `stepPickup` 的 `HOVER_DROP` case 按 `elapsed=(now−sub_hover_start_)` 分两个时刻触发（用一个 bool `drop_released_` 防重复发磁）：
     - `elapsed >= arm_extend_sec_`(1.2) 且未释放 → `publishMagnet(false)`，置 `drop_released_=true`（此刻臂已伸直，片贴到叠面再松）；
     - `elapsed >= arm_extend_sec_ + drop_settle_sec_`(1.2+0.5) → `publishServo(false)` 收臂，`++stack_count_`，进 `CLIMB_AFTER_DROP`。
- **抓取侧 `HOVER_GRAB` 不用改**：当前先磁后臂、`hover_grab_sec_`=2.0s 收臂，2.0>1.2 已覆盖伸臂耗时，且抓取是"先充磁再接触"不存在提前松的问题。
- **新参数**：`arm_extend_sec`(1.2, 机械臂伸直耗时)、`drop_settle_sec`(0.5, 松磁后片落稳再收臂)。新成员 `bool drop_released_`。

### 18.2 放置落点多压 2cm
- **需求**：放到空柱（含已叠片）上时再多下降 2cm，确保片压到叠面再松磁。**这是放空柱的下降，不是最终降地面**。
- **改法**：放置落点 z 公式加一项下压：`z_drop = R + 空柱高 + stack_count×plate_thickness + drop_gap − drop_press`。
  改 `DESCEND_DROP`（或下方 18.3 改造后的放置末段）算 z 处。
- **新参数**：`drop_press_cm`(2.0)。约定同 grab_press：值越大压得越低；放歪/压坏→回调，够不到→加大。

### 18.3 放置也走多段中停对准（复用抓取机制）
- **依据（已查视觉代码确认）**：demo3 `center_source=square`（visual_node.py:200），`/fine_data` 走**黑方框 4 角几何中心**，
  与圆无关；圆只供 `/circle_area_ratio`。所以**空柱顶只有黑框、没有圆，对准信号照常输出**，第一片放置无对准问题。
  且铁片吸在电磁铁下方**不挡**摄像头，只有机械臂伸直才挡——而放置下降全程臂是收起的 → 下降全程可视觉对框中心。
- **改法（推荐：标志位复用，不另写平行状态机）**：加成员 `bool descend_is_drop_`。
  - 新增 `startDropDescend()`：仿 `startGrabDescend()`，置 `descend_is_drop_=true`，调 `buildDescendPlan(空柱高, /*is_drop=*/true)` 建中停表，
    有中停点→进 `DESCEND_MID`，否则直接 `DESCEND_FINAL`。`startGrabDescend()` 里置 `descend_is_drop_=false`。
  - `CENTER_DROP` 对准完成后调 `startDropDescend()`（替换原来直接进 `DESCEND_DROP`）。
  - 复用的 `DESCEND_MID`/`RECENTER_MID`/`DESCEND_FINAL` 三个子状态内按 `descend_is_drop_` 切换：
    - **xy 来源**：抓取用 `survey_results_[si].x/y`（载片柱）；放置用 `empty_pillar_x/y_cm_`。
    - **末段 z 公式**：抓取 `R+柱高−grab_press`；放置 `R+空柱高+stack×厚度+drop_gap−drop_press`(见18.2)。
    - **y 偏置**：抓取末段加 `grab_final_dy_cm`(见18.5)；**放置不加**（放置是松磁释放、要对准已叠片正中，加偏置反而叠歪）。
    - **末段到位后去向**：抓取 `DESCEND_FINAL`→`HOVER_GRAB`；放置→`HOVER_DROP`。`RECENTER_MID` 递增 ckpt 的逻辑两者相同。
    - **下降安全底线**（第17节 `stepPickup` 开头 guard）：把 `in_grab_descend` 判断加 `&& !descend_is_drop_`，**放置下降不走安全底线**
      （放置 z=R+空柱高(≥50)+… ≥77，绝不会低于 22；且 abort 分支的"回 MEASURE_HEIGHT_GRAB 重试"是抓取语义，放置不能复用）。
  - `buildDescendPlan(height, is_drop)` 加 `is_drop` 形参：is_drop 时 `grab_reading` 用放置 z 公式、且 **seg 固定 30 不加密**（见18.4）。
  - 原 `DESCEND_DROP` 子状态可保留枚举但不再用（或直接删，改走 DESCEND_MID/FINAL 复用路径）；`descend_seg_len`/`recenter_timeout` 复用抓取同值。
- **⚠️ 实飞风险（叠片对框检测）**：叠第 2、3 片时柱顶已有黑铁片，黑框内"洞"从纯白变白环。
  空柱框(~30cm)≫最大片(11cm)，白环面积通常仍够、`hole_ratio≥min_hole_ratio_for_border`(0.08, detector_core.py:27/497) 仍成立 → 大概率仍锁框；
  万一被填到 hole_ratio<0.08 框丢，visual_node 发 `[0,0]`(visual_node.py:132/233)→mission 当"已对准"往下降，空柱余量大能接住、RECENTER 超时兜底，不卡不摔。叠片那两次盯一下框检测即可，代码先不为此改视觉。

### 18.4 抓最小片分段加密（seg 20）
- **需求**：抓占比最小那片（叠在最顶、最易歪）时分段更密、多对准一次；其余片不变；**放置一律不加密**（平台大怎么放都掉不下去）。
- **改法**：`buildDescendPlan` 算 seg 时：抓取且 `pickup_iter_ == pickup_order_.size()−1`（末位=占比最小）→ 用 `descend_seg_len_min_plate_cm`(20)，
  否则用 `descend_seg_len_cm`(30)；is_drop 恒用 30。
- **新参数**：`descend_seg_len_min_plate_cm`(20)。

### 18.5 抓取末段 y 负向偏置 −2cm
- **需求**：最后对准/下降时飞机再往 map **y 负方向 −2cm**，电磁铁吸得更准（吸取点相对相机中心的残余偏置）。
- **改法**：加参数 `grab_final_dy_cm`(−2.0)，**只在抓取的 `DESCEND_FINAL`** 落点 xy（`applyArmOffsetToTarget` 之后）再叠加 `ty += grab_final_dy_cm_`。
  放在**盲降段**而不是 RECENTER 视觉对准段——视觉接管时是把画面拉向中心(0,0)、塞不进硬偏置；让视觉先对正框中心，最后盲降平移 −2 再扎下去吸。
  放置末段不加（见18.3）。轴定义见第12节：map +y ↔ 画面正左方。若实飞发现偏反向，把符号取正。

### 新参数一览（launch，默认值）
| 参数 | 默认 | 含义 |
|---|---|---|
| `arm_extend_sec` | 1.2 | 机械臂收起→伸直耗时；放置先伸臂等此时长再松磁 |
| `drop_settle_sec` | 0.5 | 放置松磁后等片落稳再收臂 |
| `drop_press_cm` | 2.0 | 放置多压量：z=R+空柱高+已叠+drop_gap−drop_press |
| `descend_seg_len_min_plate_cm` | 20.0 | 抓最小片的分段长（更密对准）；其余片/放置用 `descend_seg_len_cm`(30) |
| `grab_final_dy_cm` | −2.0 | 抓取末段盲降 y 向偏置（电磁铁吸准），仅抓取 DESCEND_FINAL |

### 本轮代码改动汇总（待编译机 colcon build）
| 文件 | 改动 |
|---|---|
| `pillar_pickup_mission_node.hpp` | 加参数成员 `arm_extend_sec_`/`drop_settle_sec_`/`drop_press_cm_`/`descend_seg_len_min_plate_cm_`/`grab_final_dy_cm_`；加状态成员 `bool descend_is_drop_`/`bool drop_released_`；`buildDescendPlan` 改签名加 `bool is_drop`；声明 `startDropDescend()` |
| `pillar_pickup_mission_node.cpp` | 18.1 `HOVER_DROP` 拆时序(伸臂→1.2s松磁→0.5s收臂)；18.2 放置 z 减 drop_press；18.3 `startDropDescend()`+`CENTER_DROP`改调它+`DESCEND_MID/RECENTER_MID/DESCEND_FINAL` 按 `descend_is_drop_` 切 xy/z/去向、安全底线 guard 加 `&&!descend_is_drop_`；18.4 `buildDescendPlan` 按是否最小片选 seg；18.5 `DESCEND_FINAL` 抓取侧叠加 `grab_final_dy_cm`；declare 新参数 |
| `pillar_pickup_mission.launch.py` | 新增 `arm_extend_sec`(1.2)/`drop_settle_sec`(0.5)/`drop_press_cm`(2.0)/`descend_seg_len_min_plate_cm`(20)/`grab_final_dy_cm`(-2.0) |


### 18.6 2026-05-23 深夜实飞后已改：视觉 PID 接管 + 空柱视觉 anchor

#### 现场问题与判断
- 日志 `log_fly/5-23-05-01` 说明旧版 mission 发布 `/visual_takeover`，但 PID 实际订阅 `/visual_takeover_active`，所以此前所谓视觉微调没有真正接管。
- 修复后日志 `log_fly/5-23-05-46` 能看到 PID 打印 `Visual takeover mode changed: active/inactive`，说明接管链路已经通。
- 新问题不是“PID 没接管”，而是放置后空柱黑框会被已放铁片干扰，第二、第三次放置时视觉可能丢框、锁错黑盘或输出异常误差。
- 空柱是特大柱，边长按 `empty_pillar_side_cm=32` 处理；其他载片柱仍按原逻辑，不把 32cm 模型套到小柱上。

#### 已改代码
- `pid_controller.cpp`：视觉 takeover 下 xy 映射修正为：图像 `dy` 控 map `x`，图像 `dx` 控 map `y`，且符号按文档“map +x ↔ 画面上、map +y ↔ 画面左”处理。
- `position_pid_controller_ground.launch.py`：视觉 PID 调温和，避免微调过猛：
  - `visual_kp_x/y = 0.03`
  - `visual_kd_x/y = 0.003`
  - `visual_pixel_deadzone = 10`
  - `visual_max_xy_velocity = 6`
- `pillar_pickup_mission_node.cpp/hpp`：
  - `publishVisualTakeover()` 同时发布 `/visual_takeover` 和 `/visual_takeover_active`。
  - 新增 `skip_largest_grab_visual_align`：最大铁片**抓取**跳过视觉微调，因为现场确认最大铁片本来抓得准，过正会影响气动导致飞机抖；最大铁片**放置**不跳过，仍要对空柱中心。
  - 新增空柱 `drop anchor`：`CENTER_DROP` 或放置中停 `drop_recenter_mid` 视觉稳定对齐后，记录当前实际 `map` 位姿为 `drop_anchor`。后续 `GOTO_DROP / DESCEND_MID / RECENTER_MID / DESCEND_FINAL / CLIMB_AFTER_DROP` 优先使用 anchor，而不是重新回到第一趟点云空柱坐标。
  - anchor 可信度保护：
    - `empty_pillar_side_cm=32.0`，默认 `drop_anchor_max_correction_cm = side/2 + 8 = 24cm`；视觉修正超过这个范围直接拒绝。
    - 已有 anchor 后单次更新限制 `drop_anchor_max_update_step_cm=8cm`，避免第二/三次视觉锁错后把 anchor 拖走。
    - 已叠放后近期 `/circle_area_ratio` 有效（默认 `drop_visual_circle_veto_sec=0.6s` 内）则拒绝更新 anchor，认为可能锁到已放铁片而不是空柱框。

#### 新增参数
| 参数 | 默认 | 含义 |
|---|---:|---|
| `skip_largest_grab_visual_align` | `true` | 最大铁片抓取跳过视觉微调；放置仍校准 |
| `empty_pillar_side_cm` | `32.0` | 空柱大柱边长，仅用于空柱 anchor 修正约束 |
| `drop_visual_anchor_enable` | `true` | 视觉确认后记录/复用空柱放置 anchor |
| `drop_anchor_max_correction_cm` | `24.0` | 首次 anchor 相对点云空柱坐标的最大允许修正 |
| `drop_anchor_max_update_step_cm` | `8.0` | 已有 anchor 后单次最大更新步长 |
| `drop_visual_circle_veto_sec` | `0.6` | 已叠放后近期看到圆盘则不信本次空柱视觉更新 |

#### 重要结论
- 现在的点云柱子检测 `/detected_pillars` 只发布 `[x,y]` 中心，没有发布边框角点/边长/拟合质量。
- `32cm` 目前已经用于空柱 anchor 的可信度约束，但还没有做“点云残边 + 32cm 方柱模型反推中心”。若后续要做，需要改 `pillar_detector_tf` 让它输出空柱几何信息，而不是只输出中心点。

### 18.7 下一轮实验待办：解耦分段视觉微调 + 改放置释放高度

#### 抓取侧：做 A/B 对比，不要默认相信分段微调
用户原话要点：**分段视觉微调不一定很管效，可以给代码做好解耦，解耦成视觉微调到中心之后就直接下降抓取，不做分段，然后做对比实验，看看哪个效果更好。**

建议下一轮把抓取下降模式做成参数，而不是继续硬绑分段：
- `grab_descend_mode = segmented`：当前方案，`DESCEND_MID → RECENTER_MID → DESCEND_FINAL`。
- `grab_descend_mode = direct_after_center`：`CENTER` 视觉对准到中心后，直接 `DESCEND_FINAL` 到抓取高度，不做中停分段视觉微调。
- 对比指标：抓取成功率、是否抖、是否被中停视觉带偏、第三小片是否需要分段。
- 初步判断：当前已经出现“没分段也能抓准”的现象，说明分段微调可能不是主因，甚至可能引入额外扰动；必须用参数化 A/B 实飞验证。

#### 放置侧：不要完全贴到空柱再松磁，改成上方释放
用户原话要点：**放的时候不要完全贴到空柱再松开电磁铁。建议在现在代码基础上，先对准一次中心，然后直接降落到空柱上方 10cm 左右，伸下机械臂，松开磁铁，然后悬停 1s，免得惯性影响。**

建议下一轮把放置释放也参数化：
- 新参数建议：
  - `drop_release_clearance_cm = 10.0`：放置末段目标不再是贴到叠面，而是叠面上方约 10cm。
  - `drop_post_release_hover_sec = 1.0`：松磁后原地悬停 1s，避免机体惯性/机械臂动作把片带偏。
  - `drop_press_cm` 可临时设回 `0.0` 或废弃为兼容参数；若采用上方释放，公式应改为 `R + 空柱高 + stack×厚度 + drop_release_clearance_cm`。
- 推荐流程：
  1. `CENTER_DROP` 视觉确认空柱中心并更新 anchor。
  2. 关闭视觉接管，直接下降到 `空柱叠面上方约 10cm`。
  3. 伸机械臂。
  4. 松磁。
  5. 悬停 `1s`。
  6. 收臂并爬升。
- 这个方案比“完全贴到空柱/叠片再松磁”更少受接触摩擦、下压、机体惯性影响；尤其第二、第三片放置时，视觉黑框已经可能被铁片干扰，越不应该在贴近状态反复追目标。

#### 下一轮建议优先级
1. 先保留当前 `drop anchor` 逻辑，因为它解决“视觉微调结果被后续目标发布抵消”的硬问题。
2. 再加 `grab_descend_mode` 做抓取分段 vs 直接下降对比。
3. 再把放置末段改为“空柱上方 10cm 释放 + 悬停 1s”，这可能比继续调 `drop_press_cm` 更符合现场现象。
